# IDE
/.idea
/.vscode
.DS_Store

/.embuild
/target
/target-*/
/packages/*/target/
/packages/*/Cargo.lock
/.*_target_*/
/tools/*/target
/tools/*/out
/tools/**/__pycache__/
/tools/**/*.py[cod]
/scripts/**/__pycache__/
/scripts/**/*.py[cod]
/tools/scene_maker/assets
/logs/
/logs/coverage/
/*.lcov
/*.profraw
.env.local
.env.*.local
.env
.scannerwork/
/.scratch/
/vendor/compiler-builtins-sysroot
# Local dependency snapshots kept for reading/diffing only. No vendored crate is
# tracked any more and nothing builds from `vendor/` — every dependency resolves
# from crates.io. If a crate ever needs vendoring again, track it and reference
# it explicitly from a manifest rather than relying on it being present here.
/vendor/esp-rom-sys-0.1.3/
/vendor/esp-sync-0.1.1/

## gh-csi settings and working directory
.gh-csi/models.toml
.gh-csi/output
