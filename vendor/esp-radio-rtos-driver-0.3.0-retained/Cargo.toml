# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.88.0"
name = "esp-radio-rtos-driver"
version = "0.3.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Task scheduler interface for esp-radio"
documentation = "https://docs.espressif.com/projects/rust/esp-radio-rtos-driver/latest/"
readme = "README.md"
keywords = [
    "esp32",
    "espressif",
    "no-std",
]
categories = [
    "embedded",
    "hardware-support",
    "no-std",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/esp-rs/esp-hal"

[[package.metadata.espressif.check-configs]]
features = []

[[package.metadata.espressif.clippy-configs]]
features = []

[features]
defmt = ["dep:defmt"]
esp32 = ["esp-sync?/esp32"]
esp32c2 = ["esp-sync?/esp32c2"]
esp32c3 = ["esp-sync?/esp32c3"]
esp32c5 = ["esp-sync?/esp32c5"]
esp32c6 = ["esp-sync?/esp32c6"]
esp32c61 = ["esp-sync?/esp32c61"]
esp32h2 = ["esp-sync?/esp32h2"]
esp32s2 = ["esp-sync?/esp32s2"]
esp32s3 = ["esp-sync?/esp32s3"]
ipc-implementations = ["esp-sync"]
log-04 = ["dep:log-04"]

[lib]
name = "esp_radio_rtos_driver"
path = "src/lib.rs"

[dependencies.cfg-if]
version = "1"

[dependencies.defmt]
version = "1.0"
optional = true

[dependencies.esp-sync]
version = "0.2.1"
optional = true

[dependencies.log-04]
version = "0.4"
optional = true
package = "log"

[target.'cfg(not(target_has_atomic = "32"))'.dependencies.portable-atomic]
version = "1"
features = ["unsafe-assume-single-core"]

[target.'cfg(target_has_atomic = "32")'.dependencies.portable-atomic]
version = "1"

[target.'cfg(target_arch = "xtensa")'.dependencies.xtensa-lx]
version = "=0.13.0"
