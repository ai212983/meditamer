# Wi-Fi/Upload Decision Ledger (Hardened + Fixed)

As of: 2026-03-05

## Purpose

Keep a compact, decision-only ledger for Wi-Fi/upload tuning so we do not rerun already-decided experiments.

Use this file as the first preflight check before any new A/B or tuning run.

For the current Wi-Fi migration strategy, also check
[Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) before starting
new root-cause work.

## How to Use

1. Check whether your knob/value pair already appears in the table below.
2. If status is `promoted`, `kept default`, `rejected`, or `non-default`, do not rerun by default.
3. Rerun only with explicit new rationale (new code path, new baseline, or user-requested reconfirmation).
4. If a knob/value is not listed here, run:

```bash
rg -n "<knob>|<value>" \
  docs/development/rfc-upload-throughput-next-phase \
  docs/development/upload-throughput-history
```

5. If the proposed work is a new `esp-radio` hook/runtime tweak, stop and verify
   that it is still allowed by
   [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md).

## Current Decisions

| Area | Decision | Status | Date | Evidence |
| --- | --- | --- | --- | --- |
| Upload chunk size (`SD_UPLOAD_CHUNK_MAX_DEFAULT`) | `65_536` is the active default and stable for bounded soak under current mitigations. | promoted | 2026-03-03 | [History part-03](./upload-throughput-history/part-03.md), [RFC part-01 step 17](./rfc-upload-throughput-next-phase/part-01.md) |
| SD SPI data clock (`MEDITAMER_SD_SPI_DATA_MHZ`) | Keep `36 MHz`; do not use `40 MHz` for default path due wider tail variance. | kept default / rejected variant | 2026-03-03 | [History part-03](./upload-throughput-history/part-03.md), [RFC part-01 step 18](./rfc-upload-throughput-next-phase/part-01.md) |
| Direct HTTP RX buffer (`HTTP_RX_BUF_TARGET`) | Keep `65_536`; reject `131_072`. | kept default / rejected variant | 2026-03-03 | [History part-05](./upload-throughput-history/part-05.md), [RFC part-01 step 30](./rfc-upload-throughput-next-phase/part-01.md), [RFC part-04 section 11.18](./rfc-upload-throughput-next-phase/part-04.md) |
| Host socket Nagle (`HOSTCTL_UPLOAD_TCP_NODELAY`) | Keep `TCP_NODELAY=1`; post-step-58 recheck (`1` vs `0`) remained near-parity with no ingress-wait gain from `0`. | kept default / rejected variant | 2026-03-04 | [History part-05](./upload-throughput-history/part-05.md), [History part-10](./upload-throughput-history/part-10.md), [RFC part-08 section 11.40](./rfc-upload-throughput-next-phase/part-08.md) |
| Cross-cycle upload client reuse (`HOSTCTL_NET_REUSE_UPLOAD_CLIENT`) | Keep as opt-in diagnostics only; not a default throughput optimization. | non-default | 2026-03-04 | [History part-06](./upload-throughput-history/part-06.md), [RFC part-05](./rfc-upload-throughput-next-phase/part-05.md) |
| Reqwest burst sender mode (`HOSTCTL_UPLOAD_DIRECT_BURST_SENDER` reqwest path) | Do not promote; throughput/variance regressed despite lower ingress waits. | rejected | 2026-03-04 | [History part-08](./upload-throughput-history/part-08.md), [RFC part-07 section 11.32](./rfc-upload-throughput-next-phase/part-07.md) |
| Direct stream burst sender (`PUT /upload` burst path) | Keep experimental only; throughput default path remains non-burst. | non-default | 2026-03-04 | [History part-08](./upload-throughput-history/part-08.md), [RFC part-01 steps 49 and 51](./rfc-upload-throughput-next-phase/part-01.md) |
| Burst-mode pacing guard (`HOSTCTL_UPLOAD_PRE_PUT_DELAY_MS`) | When burst mode is enabled, default guard remains `120 ms` to suppress retry explosions. | hardened default (burst mode only) | 2026-03-04 | [History part-08](./upload-throughput-history/part-08.md), [RFC part-01 step 49](./rfc-upload-throughput-next-phase/part-01.md) |
| Upload body-read idle timeout (`HTTP_UPLOAD_BODY_READ_TIMEOUT_MS`) | Keep `6000 ms` guard to prevent `Ready`-but-unreachable stalled body-read state. | hardened default | 2026-03-04 | [RFC part-01 step 44](./rfc-upload-throughput-next-phase/part-01.md) |
| Ingress try-drain cadence (`HTTP_INGRESS_TRY_DRAIN_INTERVAL_READS_DEFAULT`) | Keep default cadence at `2`. | promoted | 2026-03-04 | [History part-08](./upload-throughput-history/part-08.md), [RFC part-01 step 47](./rfc-upload-throughput-next-phase/part-01.md) |
| Ingress fairness defaults (`HTTP_INGRESS_COOP_YIELD_*_DEFAULT`) | Current promoted defaults are `16 KiB` and `32` reads (supersedes earlier `32 KiB/64` and older `8192/24`) based on outlier-focused tail telemetry A/B; full-gate closure is temporarily blocked by separate discovery-empty instability. | promoted (latest) | 2026-03-05 | [History part-10](./upload-throughput-history/part-10.md), [History part-11](./upload-throughput-history/part-11.md), [RFC part-09 sections 11.43 and 11.45](./rfc-upload-throughput-next-phase/part-09.md), [RFC part-01 step 63](./rfc-upload-throughput-next-phase/part-01.md) |
| Adaptive ingress fairness (`HTTP_INGRESS_ADAPTIVE_FAIRNESS`) | Keep non-default diagnostics only (`0` default, `1` enable): matched post-fix A/B showed slightly worse `req_ms`/wait tails and higher variance despite active adaptation telemetry. | non-default / not promoted | 2026-03-04 | [RFC part-07 section 11.37](./rfc-upload-throughput-next-phase/part-07.md), [RFC part-01 step 56](./rfc-upload-throughput-next-phase/part-01.md), [History part-09](./upload-throughput-history/part-09.md) |
| `Connection: close` handling | Firmware honors close hint and closes immediately after response to reduce accept re-arm delay. | hardened behavior | 2026-03-04 | [RFC part-01 step 50](./rfc-upload-throughput-next-phase/part-01.md) |
| Startup/AP readiness in acceptance workflow | Keep boot-gate fallback (`ready + ssid_seen`) and cycle-1 health hysteresis (`3` successful checks) enabled. | hardened behavior | 2026-03-04 | [History part-09](./upload-throughput-history/part-09.md), [RFC part-01 step 53](./rfc-upload-throughput-next-phase/part-01.md) |
| Listener readiness gate alignment (`wifi_link_connected + DHCP lease`) | Keep HTTP listener gate aligned with lease-based readiness; avoid blocking listener arming on transient link-flag lag when lease is valid. | hardened behavior | 2026-03-04 | [History part-09](./upload-throughput-history/part-09.md), [RFC part-07 section 11.38](./rfc-upload-throughput-next-phase/part-07.md), [RFC part-01 step 58](./rfc-upload-throughput-next-phase/part-01.md) |
| Auth-reject loop recovery | Keep hinted-candidate preservation for auth-reject class to prevent immediate snap-back loops. | fixed | 2026-03-04 | [History part-09](./upload-throughput-history/part-09.md), [RFC part-01 step 54](./rfc-upload-throughput-next-phase/part-01.md) |
| AP-dense discovery fallback semantics | Use target-candidate visibility (not generic non-zero AP scan count) to drive zero-discovery probe fallback and discovery-exhaustion reset behavior. | hardened behavior | 2026-03-04 | [History part-09](./upload-throughput-history/part-09.md), [RFC part-07 section 11.36](./rfc-upload-throughput-next-phase/part-07.md), [RFC part-01 step 57](./rfc-upload-throughput-next-phase/part-01.md) |
| Pre-connect zero-discovery handling | Short-circuit association when a full scan round reports zero APs; classify immediately as `discovery_empty` and force `scan_zero_discovery_driver_restart` recovery. | hardened behavior (pending full-gate closure) | 2026-03-05 | [History part-11](./upload-throughput-history/part-11.md), [RFC part-09 section 11.46](./rfc-upload-throughput-next-phase/part-09.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Host recover discovery-guard reset | On explicit host `NET RECOVER`, reset zero-discovery guard progress (`discovery_sweep_exhausted_streak`, `zero_discovery_hard_guard_restarts`, `force_full_channel_probe_next_scan`) to avoid stale carryover into the next cycle. | fixed | 2026-03-05 | [History part-11](./upload-throughput-history/part-11.md), [RFC part-10 section 11.50](./rfc-upload-throughput-next-phase/part-10.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Post-start scan settle (`MEDITAMER_WIFI_POST_START_SETTLE_MS`) | Keep default `800 ms`; `2000 ms` probe did not recover `scan_nonzero` and reduced only bounded-round cycle count. | kept default / rejected variant | 2026-03-05 | [History part-11](./upload-throughput-history/part-11.md), [RFC part-10 section 11.52](./rfc-upload-throughput-next-phase/part-10.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Raw broad scan bypass (`MEDITAMER_WIFI_START_RAW_SCAN_DIAG`) | `diag=1` proved raw broad scans are also all-zero (`result_count=0`), so staged scan config is not primary; keep knob off by default and focus on deeper radio/driver readiness. | diagnostic outcome / keep default off | 2026-03-05 | [History part-11](./upload-throughput-history/part-11.md), [RFC part-10 section 11.53](./rfc-upload-throughput-next-phase/part-10.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Start-readiness probe (`MEDITAMER_WIFI_START_READINESS_PROBE`) | `diag=1` showed `started=true` at all post-start checkpoints and scan entry around `~1.85s` after `start_ok`; hypothesis “scan invoked too early” rejected. Keep knob off by default. | diagnostic outcome / keep default off | 2026-03-05 | [History part-11](./upload-throughput-history/part-11.md), [RFC part-10 section 11.54](./rfc-upload-throughput-next-phase/part-10.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Force stop before start (`MEDITAMER_WIFI_FORCE_STOP_BEFORE_START`) | Rejected for root-cause branch: `on` did not restore `scan_nonzero` and introduced repeated `stop timeout=5000ms` churn; keep default `off`. | rejected variant / kept default | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-10 section 11.55](./rfc-upload-throughput-next-phase/part-10.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Scan-entry event-age introspection | Event-age fields showed scan entry is not immediate post-stop / stale-immediate post-scan edge (`sta_stop_age_ms` and `last_scan_done_age_ms` ~3.6s on restarts); hypothesis rejected. | diagnostic outcome | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-10 section 11.56](./rfc-upload-throughput-next-phase/part-10.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Post-start STA protocol reapply (`MEDITAMER_WIFI_REAPPLY_PROTOCOL_AFTER_START`) | Keep default `off`; bounded A/B (`rounds=1`) showed both `off` and `on` already pass discovery with identical counters (`scan_zero=0`, `scan_nonzero=1`, `ssid_seen=2`). `on` only confirmed successful reapply marker (`post_start_protocol_reapply result=ok profile=bgn`). | diagnostic outcome / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.57](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Init country override (`MEDITAMER_WIFI_COUNTRY_US_OVERRIDE`) | Keep default `off`; bounded A/B (`rounds=1`) showed identical discovery-empty counters for `off` and `on` (`scan_zero=42`, `scan_nonzero=0`, `ssid_seen=0`). Override toggled correctly in logs (`country_us_override=false/true`) but did not recover scans. | diagnostic outcome / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.58](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Scan-entry driver-state probe (`MEDITAMER_WIFI_SCAN_ENTRY_DRIVER_STATE_DIAG`) | Diagnostic-only knob; keep default `off`. Probe confirmed scan-entry driver state is readable and consistent (`protocol_bitmap=0x07`), and country config applies (`CN` vs `US`), but discovery-empty persisted. | diagnostic outcome / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.59](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Scan-entry direct IDF comparator (`MEDITAMER_WIFI_SCAN_ENTRY_IDF_COMPARE_DIAG`) | Diagnostic-only knob; keep default `off`. Direct IDF scan (`esp_wifi_scan_start/get_ap_num/get_ap_records`) also returned zero APs when staged scans were zero, rejecting wrapper-only scan-path bug as primary cause. | diagnostic outcome / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.60](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Scan-entry runtime-state probe (expanded `MEDITAMER_WIFI_SCAN_ENTRY_DRIVER_STATE_DIAG`) | Diagnostic-only knob; keep default `off`. Expanded probe showed stable STA runtime state (`mode/channel/PS/max_tx_power/event_mask/scan_defaults`) even during all-zero discovery cycles, so no obvious runtime-state incoherence found. | diagnostic outcome / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.61](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Early driver-state pair (`MEDITAMER_WIFI_EARLY_DRIVER_STATE_DIAG`) | Diagnostic-only knob; keep default `off`. One-shot `pre_start_driver_state` and `first_start_driver_state` showed start-time state becomes coherent (`scan_defaults_rc=0`, valid tx power/channel) before scans, yet post-start promiscuous RX can still remain zero. | diagnostic outcome / keep default off | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| First-start Wi-Fi internal log bump (`MEDITAMER_WIFI_FIRST_START_IDF_LOG_DIAG`) | Diagnostic-only knob; keep default `off`. Scoped Wi-Fi internal log level bump (`DEBUG` until first `scan_done`) proved the bump/restore path works, but clean-start blackout still showed zero post-start promiscuous RX and zero raw broad scan before any new lower-level cause surfaced. | diagnostic outcome / keep default off | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| Setup-time Wi-Fi drop/reinit (`MEDITAMER_WIFI_SETUP_REINIT_DIAG`) | Rejected for blackout recovery: forcing a one-time setup-time `wifi::new -> drop/deinit -> wifi::new` cycle regressed device liveness after flash. Bounded probe produced no serial console response and `espflash monitor` failed to reconnect/reset cleanly, so keep default `off`. | rejected variant / keep default off | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| Official C/ESP-IDF lifecycle comparison | Treat current blackout as more likely `esp-radio`/bare-metal integration specific than generic ESP-IDF Wi-Fi behavior. Official C examples use `nvs_flash_init -> esp_netif_init -> esp_event_loop_create_default -> esp_netif_create_default_wifi_sta -> esp_wifi_init -> esp_wifi_set_mode -> esp_wifi_set_config? -> esp_wifi_start`, and official tests require `esp_wifi_stop()` before `esp_wifi_deinit()`. Our Rust path uses `esp_wifi_init_internal`, immediate `WIFI_MODE_NULL`, custom RX callback registration, no `esp_netif`, and `WifiController::drop`-driven deinit. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| Cold-boot runtime setup + STA-config snapshots (`runtime_setup_driver_state`, `pre_start_sta_config`, `first_start_sta_config`) | Treat high-level runtime config as non-primary for current blackout: cold boot shows sane `mode=0/protocol_bitmap=0x07/cc=CN`, and blackout runs still show sane STA config (`ssid_len=19`, `bssid_set=false`, `channel=0`, `threshold_authmode=4`) before and after `start_ok` while post-start promisc and scans remain all-zero. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| Scan-entry promiscuous RX sweep (`MEDITAMER_WIFI_SCAN_ENTRY_PROMISC_DIAG`) | Diagnostic-only knob; keep default `off`. Channel sweep (`8/1/6/11`) reported zero promiscuous RX packets in failing cycles while staged scans were also zero, indicating deeper radio RX-ingress blackout behavior rather than scan-wrapper-only failure. | diagnostic outcome / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.62](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Post-stop Wi-Fi MAC reset (`MEDITAMER_WIFI_RESET_MAC_AFTER_STOP`) | Rejected for blackout recovery: `reset_wifi_mac_after_stop applied` executed, but bounded run remained `scan_nonzero=0` and promiscuous sweep stayed zero. Keep default `off`. | rejected variant / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.65](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| `esp_wifi_restore()` post-stop recovery branch | Rejected: attempted as deeper recovery primitive but triggered runtime panic (`not yet implemented: misc_nvs_restore` in `esp-radio`), so do not retry in current stack path. | rejected branch | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.63](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Post-stop mode bounce (`MEDITAMER_WIFI_MODE_NULL_STA_RESET_AFTER_STOP`) | Rejected for root-cause branch: `NULL -> STA` mode bounce executed successfully (`set_null_rc=0`, `set_sta_rc=0`) but bounded A/B remained all-zero (`scan_nonzero=0`). Keep default `off`. | rejected variant / keep default off | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.67](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Zero-discovery hard-guard software reset (`MEDITAMER_WIFI_SOFTWARE_RESET_ON_ZERO_DISCOVERY_HARD_GUARD`) | Experimental recovery path: firmware can intentionally software-reset on hard-guard trip to break all-zero restart loops; requires host expected-reboot classing to avoid false panic abort. Keep non-default while validation continues. | non-default / under validation | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 sections 11.70 and 11.71](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Post-start promiscuous-zero software reset (`MEDITAMER_WIFI_SOFTWARE_RESET_ON_POST_START_PROMISC_ZERO`) | Experimental recovery path: if immediate post-start promiscuous RX sweep is zero across sampled channels, reboot before spending a full discovery cycle. Initial bounded repro showed host classing works, but blackout reproduced again immediately after reboot, so this is not a confirmed fix. | non-default / under validation | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| Post-start promiscuous-zero deep reinit (`MEDITAMER_WIFI_POST_START_PROMISC_ZERO_HARD_REINIT`) | Rejected for blackout recovery: forced stop + `reset_wifi_mac()` + `WIFI_MODE_NULL -> WIFI_MODE_STA` immediately after post-start promisc-zero still reproduced `total=0` on every restart in the same boot. Keep default `off`. | rejected variant / keep default off | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| RAM-only Wi-Fi storage (`MEDITAMER_WIFI_FORCE_STORAGE_RAM`) | Rejected for blackout recovery: boot monitor confirmed `wifi_storage_override mode=ram rc=0`, but immediate post-start promiscuous RX and discovery scans still stayed all-zero. Keep default `off`. | rejected variant / keep default off | 2026-03-06 | [History part-14](./upload-throughput-history/part-14.md) |
| C-like discovery start (`MEDITAMER_WIFI_C_LIKE_DISCOVERY_START`) | Rejected for blackout root-cause: bare `WIFI_MODE_STA -> start_async()` with empty STA config still showed zero post-start promiscuous RX and zero raw broad scan before any station config was applied. Keep default `off`. | rejected variant / keep default off | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [C control scan log](../../logs/esp_idf_wifi_control_scan_20260306_100150.log) |
| Boot scan-only Rust repro (`MEDITAMER_WIFI_BOOT_SCAN_ONLY_DIAG`) | Diagnostic-only knob; keep default `off`. Minimal Rust `esp-radio` path (`set_mode(STA) -> start -> broad scan -> stop`) still produced `result_count=0` before any `NETCFG` or upload connection-state logic ran. Direct IDF scan inside that same started session also returned `ap_num=0`, so the blackout reproduces below both the upload state machine and the Rust scan wrapper. | diagnostic outcome / keep default off | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [boot scan log](../../logs/boot_scan_only_diag_20260306_091740/boot_espflash_monitor.log), [boot scan + IDF compare log](../../logs/boot_scan_only_diag_idfcompare_20260306_092204/boot_espflash_monitor.log), [C control scan log](../../logs/esp_idf_wifi_control_scan_20260306_100150.log) |
| Local `esp-radio` init-path single-delta A/Bs (`skip_internal_rxcb`, `skip_supplicant_init`, `skip_initial_mode_null`, `use_public_init`) | No-effect experiments in the local dependency copy: each variant still produced zero Rust boot scan and zero direct IDF scan in the same started session. These are not individually sufficient root causes. | diagnostic outcome / rejected as sole cause | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [skip-rxcb log](../../logs/boot_scan_only_diag_skiprxcb_20260306_092929/boot_espflash_monitor.log), [skip-supplicant log](../../logs/boot_scan_only_diag_skipsupp_20260306_093024/boot_espflash_monitor.log), [skip-null log](../../logs/boot_scan_only_diag_skipnull_20260306_093115/boot_espflash_monitor.log), [public-init log](../../logs/boot_scan_only_diag_publicinit_20260306_093405/boot_espflash_monitor.log) |
| Direct C lifecycle preinit inside current Rust firmware | Blocked, not adopted: attempting `esp_netif_init -> esp_event_loop_create_default -> esp_netif_create_default_wifi_sta` inside the current no-`esp-idf` Rust image requires a much wider ESP-IDF/FreeRTOS link surface than this firmware provides. Treat this as an architectural boundary, not a missing call-site. | diagnostic outcome / blocked by link model | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [C control scan log](../../logs/esp_idf_wifi_control_scan_20260306_100150.log) |
| Separate Rust-on-ESP-IDF control probe (`tools/esp_idf_wifi_control_rust`, `scripts/device/wifi_control_idf_rust.sh`) | Preferred next comparator path. Probe scaffold is in place, but current crate set (`esp-idf-svc 0.51`, `esp-idf-hal 0.45.2`) does not build cleanly against ESP-IDF 5.5.2; use a supported ESP-IDF 5.3.x install for this path. | diagnostic path / blocked by current IDF version | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [C control scan log](../../logs/esp_idf_wifi_control_scan_20260306_100150.log) |
| Separate Rust-on-ESP-IDF control probe (`tools/esp_idf_wifi_control_rust`, `scripts/device/wifi_control_idf_rust.sh`) | Probe now builds and scans successfully against external ESP-IDF `v5.3.4` on the same board/network. This rules out Rust-in-general and ESP-IDF Wi-Fi stack behavior as the dominant cause, and narrows the blackout boundary to the current `esp-radio` / no-`esp-idf` initialization or integration path. | diagnostic outcome / root-cause boundary pinned | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [Rust-on-IDF scan log](../../logs/esp_idf_wifi_control_rust_scan_20260306_111949.log), [C control scan log](../../logs/esp_idf_wifi_control_scan_20260306_100150.log), [boot scan + IDF compare log](../../logs/boot_scan_only_diag_idfcompare_20260306_092204/boot_espflash_monitor.log) |
| Clean published `esp-radio 0.17.0` baseline recheck | Restoring the local Cargo-registry copy of `esp-radio` to the published crate contents did not change the failing shape: the minimal boot scan still returned `result_count=0` and direct IDF compare still returned `ap_num=0`. Dirty local dependency state was not the primary cause. | diagnostic outcome / rejected as primary cause | 2026-03-06 | [History part-15](./upload-throughput-history/part-15.md), [clean baseline boot scan log](../../logs/boot_scan_only_diag_clean_esp_radio_20260306_113043.log), [restore backup](../../logs/esp_radio_restore_20260306_112919/mod.rs.restore.diff) |
| Official ESP-IDF C control app with `wifi_init_config_t.nvs_enable = 0` | Scan-only control app still discovers APs with Wi-Fi NVS disabled (`wifi_init nvs_enable=0`, `scan_complete total_ap_count=7`). This rejects `nvs_enable=0` as a sufficient cause of the `esp-radio` blackout. | diagnostic outcome / rejected as sole cause | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [NVS-off control log](../../logs/esp_idf_wifi_control_nvs_off_20260306_115003/monitor.log) |
| Discovery workflow expected-reboot handling (hostctl) | Discovery workflow suppresses one marker-armed `UnexpectedReboot` (`software_reset=true` path) and re-applies `NETCFG` + `NET START` instead of hard panic abort. | hardened diagnostics behavior (discovery workflow) | 2026-03-05 | [History part-12](./upload-throughput-history/part-12.md), [RFC part-11 section 11.71](./rfc-upload-throughput-next-phase/part-11.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Strict per-round scan evidence gate (host discovery profile) | Keep strict mode available and preferred for bounded regression (`require_scan_evidence_each_round=true`); evidence is counted from both raw scan events and `METRICS WIFI` per-round deltas (`scan_runs_delta`) so ready-only rounds with zero scan activity cannot pass. | hardened regression gate behavior | 2026-03-05 | [History part-13](./upload-throughput-history/part-13.md), [RFC part-01 steps 64 and 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Host control-command ack-loss guard (discovery workflow) | Keep guarded pre-round control path (`NET STOP/RECOVER/LISTENER OFF/START`) with one recovery retry and explicit host classification `host_transport_control_ack_loss`; prevents silent drift in unstable rounds and restores deterministic failure mode. | hardened behavior | 2026-03-05 | [History part-13](./upload-throughput-history/part-13.md), [RFC part-01 step 65](./rfc-upload-throughput-next-phase/part-01.md) |
| Host transport-reset fast retry (`HOSTCTL_UPLOAD_TRANSPORT_RESET_FAST_RETRY`, `HOSTCTL_UPLOAD_TRANSPORT_RESET_FAST_RETRY_STREAK`) | Keep enabled by default (`1`, streak `2`): short-circuit early transport-reset retries with fresh client + short backoff to collapse AP-contention per-cycle outlier tails while preserving full recovery wait for longer reset streaks. | hardened default | 2026-03-05 | [History part-13](./upload-throughput-history/part-13.md), [RFC part-01 step 66](./rfc-upload-throughput-next-phase/part-01.md) |
| Direct-mode repeated-reset chunk fallback (`HOSTCTL_UPLOAD_TRANSPORT_RESET_CHUNK_FALLBACK`, `HOSTCTL_UPLOAD_TRANSPORT_RESET_CHUNK_FALLBACK_STREAK`) | Keep enabled by default (`1`, streak `2`): when direct `PUT /upload` reset streak exceeds limit, degrade in the same cycle to chunked upload with fresh client handoff (`fresh_client=1`) instead of hard-failing cycle; deterministic host test coverage now enforces `/upload_abort -> /upload_begin -> /upload_chunk -> /upload_commit` fallback path. | hardened default | 2026-03-05 | [History part-13](./upload-throughput-history/part-13.md), [RFC part-01 steps 67 and 68](./rfc-upload-throughput-next-phase/part-01.md) |
| Acceptance fresh-client refresh retry (`HOSTCTL_NET_UPLOAD_REFRESH_ON_FAILURE`) | Promote default to `on` (`1`) and include `host_transport_connection_reset` in retry-eligible host classes (alongside send/health-send failures); keeps one bounded same-cycle fresh-client retry before cycle fail to reduce transient host-side upload aborts while preserving classed diagnostics. | hardened default | 2026-03-05 | [History part-13](./upload-throughput-history/part-13.md), [RFC part-01 step 69](./rfc-upload-throughput-next-phase/part-01.md) |
| Acceptance startup listener/readiness hardening (`HOSTCTL_NET_ENSURE_OPERATING_MODE`, `HOSTCTL_NET_LISTENER_READY_GRACE_MS`, `HOSTCTL_NET_FORCE_STOP_BEFORE_RECOVER`) | Keep defaults enabled: normalize state to operating upload mode before run (`STATE SET upload=on`, `STATE DIAG kind=NONE`), allow short listener-ready grace, and perform `NET STOP` before forced `NET RECOVER` on startup listener churn; mitigates recurring pre-upload `listener_not_ready` class after discovery stage. | hardened default | 2026-03-05 | [History part-13](./upload-throughput-history/part-13.md), [RFC part-01 step 70](./rfc-upload-throughput-next-phase/part-01.md) |
| Acceptance `net_wait_ready` bounded recover retry (`HOSTCTL_NET_WAIT_READY_RECOVER_RETRIES`) | Keep default `1`: on readiness failure signatures (`listener_not_ready`, listener timeout, DHCP no-IPv4 stall), run one host recover+restart retry before terminal fail classification. Hardening reduces single-shot false aborts but does not yet eliminate underlying firmware-side listener/DHCP stall in AP-dense conditions. | hardened default / root-cause still open | 2026-03-05 | [History part-14](./upload-throughput-history/part-14.md), [RFC part-12 section 12.1](./rfc-upload-throughput-next-phase/part-12.md), [RFC part-01 step 71](./rfc-upload-throughput-next-phase/part-01.md) |
| Post-flash lease-sync finding in listener stall | Treat lease-loss as non-dominant for current failure class: strict failing-window repro shows `dhcp_ready lease_ipv4=...` and repeated `ListenerWait` with non-zero `ipv4` while listener stays false until timeout. | diagnostic outcome / root-cause narrowed | 2026-03-05 | [History part-14](./upload-throughput-history/part-14.md), [RFC part-12 section 12.2](./rfc-upload-throughput-next-phase/part-12.md), [RFC part-01 step 72](./rfc-upload-throughput-next-phase/part-01.md) |
| HTTP listener gate transition diagnostics (`transfers_paused`, `listener_gate reason/clear`) | Keep enabled via NET diagnostics; failing acceptance window confirmed non-transfer suppression path (repeated lease-ready `ListenerWait -> listener_timeout`) and provided root-cause evidence for listener re-arm mitigation. | diagnostic instrumentation retained | 2026-03-05 | [History part-14](./upload-throughput-history/part-14.md), [RFC part-12 section 12.3](./rfc-upload-throughput-next-phase/part-12.md), [RFC part-01 step 73](./rfc-upload-throughput-next-phase/part-01.md), [Batch run1 acceptance](../../logs/wifi_regression_batch_20260305_185110/run_1/acceptance_1_cycle.log) |
| HTTP accept gate-aware polling re-arm (`ACCEPT_POLL_MS=500`) | Promote mitigation: accept loop now polls with gate checks and aborts stale sockets on gate-loss, preventing prolonged listener re-arm stalls under reconnect churn. Strict post-flash AP-contention confirmation batch (3 runs) stayed stable with zero `listener_timeout`/`listener_not_ready` and zero `accept_link_reset`. | hardened behavior | 2026-03-05 | [History part-14](./upload-throughput-history/part-14.md), [RFC part-12 sections 12.4 and 12.6](./rfc-upload-throughput-next-phase/part-12.md), [RFC part-01 step 74](./rfc-upload-throughput-next-phase/part-01.md), [Gate 20260305_190833](../../logs/wifi_regression_gate_20260305_190833/report.json), [Gate 20260305_191234](../../logs/wifi_regression_gate_20260305_191234/report.json), [Batch 20260305_193824](../../logs/wifi_apcontention_confirm_20260305_193824/summary.tsv) |
| Listener-timeout hard-recover guard (`streak>=6` or `internal_free_drop>=1024B`) | Keep enabled: escalate repeated listener-timeout loops from disconnect-only retry to stop/start hard recover with candidate/hint reset to cap internal-memory drift before attempt-budget exhaustion. AP-contention confirmation batch passed with no regressions; explicit guard-trigger-path proof remains non-blocking follow-up. | hardened behavior | 2026-03-05 | [History part-14](./upload-throughput-history/part-14.md), [RFC part-12 sections 12.5 and 12.6](./rfc-upload-throughput-next-phase/part-12.md), [RFC part-01 step 74](./rfc-upload-throughput-next-phase/part-01.md), [Gate 20260305_193136](../../logs/wifi_regression_gate_20260305_193136/report.json), [Batch 20260305_193824](../../logs/wifi_apcontention_confirm_20260305_193824/summary.tsv) |
| `esp-radio` Wi-Fi `is_from_isr()` adapter predicate (`MEDITAMER_WIFI_ESP_RADIO_USE_REAL_ISR_CHECK`) | Keep non-default diagnostic only: switching Wi-Fi ISR-context reporting from unconditional `true` to `crate::is_interrupts_disabled()` did not restore AP visibility in the minimal boot scan repro. | rejected variant / diagnostic retained | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [boot-scan real-isr log](../../logs/boot_scan_only_diag_realisr_20260306_120359/boot_espflash_monitor.log) |
| `esp-radio` IDF-like init defaults without NVS (`MEDITAMER_WIFI_ESP_RADIO_USE_IDF_INIT_DEFAULTS_NO_NVS`) | Keep non-default diagnostic only: applying the rest of the IDF-style `wifi_init_config_t` defaults while forcing `nvs_enable=0` still produced zero-result Rust and direct-IDF scans in the minimal boot repro. | rejected variant / diagnostic retained | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [boot-scan idf-defaults-no-nvs log](../../logs/boot_scan_only_diag_idfdefaults_nonvs_20260306_120359/boot_espflash_monitor.log) |
| `esp-radio` `WIFI_MAC` ISR liveness counter | Keep diagnostic retained: failing zero-scan baseline still receives `WIFI_MAC` interrupts (`27` by Rust scan completion, `53` by direct-IDF compare), so root-cause focus moves above raw interrupt delivery. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [boot-scan ISR-count log](../../logs/boot_scan_only_diag_isrcount_20260306_120359/boot_espflash_monitor.log) |
| `esp-radio` Wi-Fi OS handoff counters | Keep diagnostic retained: failing zero-scan baseline still shows active semaphore and queue traffic during the scan window, so generic OS handoff starvation is not sufficient to explain the blackout. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [boot-scan OS-diag log](../../logs/boot_scan_only_diag_osdiag_20260306_120359/boot_espflash_monitor.log) |
| `esp-radio` STA/AP RX callback counters | Keep diagnostic retained: failing zero-scan baseline still reaches non-zero `WIFI_MAC` interrupt and OS handoff counts while STA/AP RX callback counts remain `0`; useful for narrowing, but not conclusive because these callbacks may be data-path specific rather than scan-result callbacks. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [boot-scan RX-callback log](../../logs/boot_scan_only_diag_rxcb_20260306_120359/boot_espflash_monitor.log) |
| `esp-radio` `ScanDone` event-post AP snapshot | Keep diagnostic retained: failing zero-scan baseline already has `number=0` and `ap_num=0` at the `ScanDone` event-post boundary, so visibility is not being lost later in `scan_results()` or firmware-side event handling. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [boot-scan event-post log](../../logs/boot_scan_only_diag_eventpostdiag_20260306_120359/boot_espflash_monitor.log) |
| Pre-scan driver-state compare (`C/IDF`, `Rust-on-IDF`, no-std `esp-radio`) | Working control paths agree on visible pre-scan state (`ps=1`, country `01.` auto, `max_tx_power=78`), while the failing no-std `esp-radio` path differs before the first scan (`ps=0`, country `CN.` manual, `max_tx_power=80`) even though scan defaults match. Treat those visible deltas as the next causality target. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [C control state log](../../logs/esp_idf_wifi_control_scan_state_20260306_berlin.log), [Rust-on-IDF state log](../../logs/esp_idf_wifi_control_rust_scan_state_20260306_berlin.log), [no-std boot-scan state log](../../logs/boot_scan_only_diag_statecmp_20260306_berlin.log) |
| C/ESP-IDF causality check for `ps=0` + `country=CN/manual` | Forcing the working C control app to `WIFI_PS_NONE` and `CN/manual` still yields healthy scan results (`7` APs), so those visible pre-scan deltas are not sufficient to reproduce blackout. Pre-start `esp_wifi_set_max_tx_power(80)` is also invalid in that call order (`ESP_ERR_WIFI_NOT_STARTED`). | diagnostic outcome / insufficient-cause proven | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [state-match PS+country log](../../logs/esp_idf_wifi_control_scan_state_match_ps_country_20260306_berlin.log), [state-match all-three log](../../logs/esp_idf_wifi_control_scan_state_match2_20260306_berlin.log) |
| `esp-radio` blob-tick base (`MEDITAMER_WIFI_ESP_RADIO_BLOB_TICK_10MS`) | Keep non-default diagnostic only: changing vendored `esp-radio` blob tick from `1 ms` to `10 ms` did not alter the minimal boot-scan failure shape or elapsed time meaningfully; direct-IDF compare remained `ap_num=0` and Rust scan remained `result_count=0`. | rejected variant / diagnostic retained | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [tick-10ms boot-scan log](../../logs/boot_scan_only_diag_tick10ms_20260306_berlin.log) |
| `esp-radio` adapter primitive usage in failing boot-scan | Failing boot-scan path does not touch event-group callbacks at all (`event_group_* = 0`), but does exercise thread-semaphore fetches and task delay / ms-to-tick conversions (`thread_sem_get=10`, `task_delay_count=16`, `task_ms_to_tick_count=104` by Rust scan completion). This shifts next focus to the thread-semaphore / queue / task-runtime contract. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [adapter-diag boot-scan log](../../logs/boot_scan_only_diag_adapterdiag_20260306_berlin.log) |
| no-std wait-queue wake behavior (`MEDITAMER_WIFI_ESP_RTOS_WAIT_QUEUE_NOTIFY_ONE`) | Keep non-default diagnostic only: switching no-std wait queues from wake-all to wake-one did not change the minimal boot-scan failure shape; Rust scan stayed `result_count=0`, direct-IDF compare stayed `ap_num=0`, and `ScanDone` event-post still reported `number=0 ap_num=0`. | rejected variant / diagnostic retained | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [waitq-notify-one boot-scan log](../../logs/boot_scan_only_diag_waitqnotifyone_20260306_130829.log) |
| no-std queue registration baseline (`register_queue_implementation!(Queue)`) | Restore published `esp-rtos` queue registration and keep it as the baseline; removing the vendored `CompatQueue` divergence did not change the minimal boot-scan blackout (`result_count=0`, `ap_num=0`). | normalized baseline / divergence removed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [queue-baseline boot-scan log](../../logs/boot_scan_only_diag_queuebaseline_20260306_131552.log) |
| no-std Wi-Fi thread-semaphore identity | Keep diagnostic retained: during the failing scan window the Wi-Fi thread-semaphore path stays pinned to a single task and a single semaphore pointer (`thread_sem_ptr_changes=0`, `thread_sem_task_changes=0`), so root-cause focus should move away from semaphore-identity churn. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [thread-semaphore identity boot-scan log](../../logs/boot_scan_only_diag_semid_20260306_131851.log) |
| no-std queue-task samples | Keep diagnostic retained: the failing scan path is dominated by a single queue handle (`0x3ffb5d00`) with a stable sampled receiver task (`0x3ffb77a0`) and an initially stable sampled sender task (`0x3ffb3470`), while aggregate counters still show later sender-task churn on that same queue (`queue_send_last_task_ptr=0x3ffc0250`). | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [queue-sample boot-scan log](../../logs/boot_scan_only_diag_qsamples_20260306_132613.log) |
| esp-rtos task-create role mapping (`MEDITAMER_WIFI_ESP_RTOS_TASK_CREATE_DIAG`) | Keep diagnostic retained but unresolved: create-site mapping logs did not surface in the bounded boot capture, so task-role attribution for the queue-task pointers remains open. | diagnostic outcome / unresolved | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [task-map boot-scan log](../../logs/boot_scan_only_diag_taskmap_warn_20260306_133117.log) |
| no-std queue task-role mapping | Keep diagnostic retained: the dominant failing queue is now role-resolved as `main -> wifi`, with later send-side churn reaching `timer` on the same queue. This pins the next root-cause target to the `timer -> queue -> wifi` interaction on that queue. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [queue-role boot-scan log](../../logs/boot_scan_only_diag_taskroles_20260306_133550.log) |
| dominant queue payload shape | Keep diagnostic retained: the dominant `main/timer -> wifi` queue carries `8-byte` messages, with late failing-window timer payload shape `(word0=0x00000007, word1=0x3ffb7934)` and earlier main-side shape `(0x00000006, 0x3ffbe114)`. Treat this as the next mapping target on the timer producer path. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [queue-payload boot-scan log](../../logs/boot_scan_only_diag_qpayload_20260306_133948.log) |
| timer-side dominant queue producer | Keep diagnostic retained: the late failing-window timer sender is specifically `cnx_connect_timeout` (`0x40183fc0`), which calls `ieee80211_timer_process` with leading argument `7`; this aligns with the observed queue payload tag `word0=0x00000007`. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [timerdiag boot-scan log](../../logs/boot_scan_only_diag_timerdiag_20260306_134431.log) |
| main-side dominant queue producer | Keep diagnostic retained: the main-side `word0=0x00000006` dominant queue item is `pp_post(6, ioctl_ptr)` from `ieee80211_ioctl`, and the sampled last pointee after the failing Rust scan resolves to `wifi_clear_ap_list_process` (`0x40180768`). Treat this as proof that control/ioctl queueing is live; next target is the short handler sequence around scan start rather than generic queue delivery. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [pointee boot-scan log](../../logs/boot_scan_only_diag_pointee_20260306_1400.log) |
| early main-side `word0=6` handler sequence | Keep diagnostic retained: the first sampled `main -> wifi` dominant queue commands resolve to `wifi_set_mode_process -> wifi_start_process -> wifi_ipc_process -> wifi_get_channel_process -> wifi_get_protocol_process -> wifi_get_scan_params_process`, while the last sampled main-side control item by Rust-scan end is `wifi_clear_ap_list_process`. Treat this as evidence that the coarse ioctl/control path is alive and ordered before the blackout manifests. | diagnostic outcome / root-cause narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [cmd-seq boot-scan log](../../logs/boot_scan_only_diag_cmdseq_20260306_1403.log) |
| recent-send ring across timer-side tail | Keep diagnostic retained: the recent dominant queue tail interleaves `wifi`-task items (`word0=0x10` / `0x00`) with `timer`-task `word0=0x07` messages whose sampled small payload state is `pointee_word1=0` or `1`, while the last `main`-task `word0=0x06` item still resolves to `wifi_clear_ap_list_process`. Treat this as evidence that the later timer-driven queue family is structurally different from the main-side ioctl objects and should be the next semantic target. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [timer-ring boot-scan log](../../logs/boot_scan_only_diag_timerring_20260306_1407.log) |
| timer-side payload state shape | Keep diagnostic retained: the `timer`-task `word0=0x07` messages carry a stable compact payload `(pointee_word0=0x00000008, pointee_word1=0x00000000|0x00000001)`, while the `main`-task `word0=0x06` items remain handler-bearing ioctl objects. Treat `(8,0)->(8,1)` as the next semantic state transition to map against `ScanDone(count=0)` and early connect-timeout behavior. | diagnostic outcome / next-step narrowed | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [timer-state boot-scan log](../../logs/boot_scan_only_diag_timerstate_20260306_1414.log) |
| execution-time timer callback correlation for queue sends | Keep diagnostic retained: the timer-side `(word0=7, pointee_word0=8, pointee_word1=0|1)` queue sends are emitted while `0x401718c8` is executing, which resolves to `nan_dp_schedule_ndc_start + 0x68` in the current build, not `cnx_connect_timeout`. `timer_compat_diag` still shows `cnx_connect_timeout` as the last armed callback, so the earlier attribution was wrong and the next focus should move to the NAN/channel-scheduler timer family active during the failing scan window. | diagnostic outcome / root-cause boundary corrected | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [timer-correlation boot-scan log](../../logs/boot_scan_only_diag_timercorr_retry2_20260306_berlin/boot_espflash_monitor.log) |
| timer `setfn/arm` history in failing boot-scan | Keep diagnostic retained: the recent timer arm ring is dominated by `0x40172174` with args `1` and `0` at `120000/20000/10000 us`, and symbol resolution places that callback in the `nan_dp_schedule_ndc_start` family. `cnx_connect_timeout` still appears as the last registered callback, but it does not dominate the actual re-arm traffic in the zero-scan window. | diagnostic outcome / root-cause boundary corrected | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [timer-ring2 boot-scan log](../../logs/boot_scan_only_diag_timerring2_20260306_berlin/boot_espflash_monitor.log) |
| suppress `nan_dp_schedule_ndc_start` timer-family arms (`MEDITAMER_WIFI_SUPPRESS_NAN_DP_TIMER_ARM_DIAG`) | Keep default `off`; diagnostic-only suppression materially changes the failing shape. With `on`, the timer-side `(word0=7, pointee_word0=8, pointee_word1=0|1)` queue family disappears entirely, `suppressed_arm_count` increments on callbacks still resolving into the `nan_dp_schedule_ndc_start` family, and scan failure shifts earlier (`idf_compare_first scan_done_eventpost count=0`, Rust scan `elapsed_ms=8`, `status=1`). This is causal evidence, not a production fix. | diagnostic outcome / strong causal signal / keep default off | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [nan-suppress boot-scan log](../../logs/boot_scan_only_diag_nansuppress_20260306_berlin/boot_espflash_monitor.log) |
| suppress only `nan_dp_schedule_ndc_start(arg=1)` arms (`MEDITAMER_WIFI_SUPPRESS_NAN_DP_TIMER_ARG1_ARM_DIAG`) | Keep default `off`; diagnostic-only split probe. With only `arg=1` suppressed, the early failure shape already shifts (`idf_compare_first scan_done_eventpost count=0`, Rust scan `elapsed_ms=8`, `status=1`), but the later timer-side `(word0=7, pointee_word0=8, pointee_word1=0)` queue family still remains. This shows the `chm_init`-registered `arg=1` and `arg=0` branches are not equivalent: `arg=1` is sufficient for the early phase change, while `arg=0` still drives the later surviving timer traffic. | diagnostic outcome / branch split clarified / keep default off | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [nan-arg1 boot-scan log](../../logs/boot_scan_only_diag_nanarg1_20260306_berlin/boot_espflash_monitor.log) |
| suppress only `nan_dp_schedule_ndc_start(arg=0)` arms (`MEDITAMER_WIFI_SUPPRESS_NAN_DP_TIMER_ARG0_ARM_DIAG`) | Keep default `off`; diagnostic-only split probe. With only `arg=0` suppressed, the original blackout shape largely survives: direct-IDF compare still reports `count=1 status=0 scan_id=128`, Rust scan still reports `elapsed_ms=309 result_count=0` and `count=2 status=0`, and timer-side `(word0=7, pointee_word0=8, pointee_word1=1)` traffic remains. This makes `arg=1` the dominant causal branch and `arg=0` the secondary later-phase branch. | diagnostic outcome / branch split clarified / keep default off | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [nan-arg0 boot-scan log](../../logs/boot_scan_only_diag_nanarg0_20260306_berlin/boot_espflash_monitor.log) |
| suppress only `nan_dp_schedule_ndc_start(arg=1)` at registration (`MEDITAMER_WIFI_SUPPRESS_NAN_DP_TIMER_ARG1_SETFN_DIAG`) | Keep default `off`; diagnostic-only registration-time probe. Replacing only the `arg=1` timer registration with a no-op callback reproduces the same early phase change as the later `arg=1` arm suppression: `idf_compare_first scan_done_eventpost count=0 scan_id=0`, Rust scan `elapsed_ms=8`, `status=1`, and only the later `(word0=7, pointee_word0=8, pointee_word1=0)` timer traffic remains. This pins the dominant causal branch at `chm_init` registration time. | diagnostic outcome / dominant branch pinned / keep default off | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [nan-arg1-setfn boot-scan log](../../logs/boot_scan_only_diag_nanarg1setfn_20260306_berlin/boot_espflash_monitor.log) |
| wrapper-arm caller trace for surviving NAN timer traffic | Keep diagnostic retained: recording the caller PC at `common_adapter::ets_timer_arm[_us]` shows the alternating surviving `10000/20000 us` timer arms return to `0x80172f85` and `0x80172fb5`, which disassemble to the two OSI timer-arm call sites inside `chm_init` itself (`callx8` at `0x40172f71` and `0x40172fa1`). Treat this as proof that the surviving timer branch is armed directly from `chm_init`, not from a later generic timer worker. | diagnostic outcome / root-cause boundary tightened | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [nan-arg1-setfn wrapper-caller boot-scan log](../../logs/boot_scan_only_diag_nanarg1setfn_wrappercallers_20260306_berlin/boot_espflash_monitor.log) |
| static start-path check for `nan_start` vs `wifi_hw_start -> chm_init` | Treat explicit `nan_start` as non-primary for the STA blackout path. Static disassembly shows `_do_wifi_start` calls `nan_start` only for mode `4`, while the ordinary start path reaches `chm_init` directly through `wifi_hw_start` (`wifi_reset_mac -> ic_mac_init -> chm_init -> chip_enable`). This shifts the next root-cause target to the state/preconditions passed into normal hardware start, not to an accidental explicit `nan_start` call in STA mode. | diagnostic outcome / root-cause boundary tightened | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md) |
| raw `g_ic` / STA / `g_chm` state probe at failing boot-scan checkpoints | Keep diagnostic retained: bounded live snapshots show `g_ic` flags and the STA object remain stable between `idf_compare_first` and the later failing Rust scan, while the `g_chm` channel-manager state changes materially. Static field mapping shows the changing bytes are the active-op slot (`+0x04/+0x05`) and current channel pair (`+0x52/+0x53`), which move from channel `1` to channel `4`, with op metadata changing from `0x0/0x78` to `0x0a/0x14`. Treat this as evidence that the early blackout is more tightly coupled to `chm` state evolution than to gross STA-object or top-level `g_ic` instability. | diagnostic outcome / root-cause boundary tightened | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [blob-state boot-scan log](../../logs/boot_scan_only_diag_blobstate_20260306_berlin_retry1/boot_espflash_monitor.log) |
| static `chm_start_op` caller classification against the live `g_chm` active-op record | Treat the retry1 `g_chm` active-op metadata as scan-path state, not direct NAN/NDC state. Static disassembly shows `chm_start_op` stores caller metadata directly into `g_chm`, and the live record shape from retry1 matches `clear_bss_queue -> chm_start_op` much better than `nan_dp_indicate_ndc_start`: small scalar metadata in `+0x08/+0x0c`, a stable scan-local context pointer near the `app_scan_params` / `connect_scan_flag` region at `+0x10`, and a stable callback near the scan handler region at `+0x14`. `scan_inter_channel_timeout_process` also feeds back into `clear_bss_queue`, which is consistent with the observed `current_chan` advance from `1` to `4` during the blacked-out window. | diagnostic outcome / root-cause boundary tightened | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [blob-state boot-scan log](../../logs/boot_scan_only_diag_blobstate_20260306_berlin_retry1/boot_espflash_monitor.log) |
| exact ESP32 OSI slot at `g_osi_funcs_p + 0x194` | Treat `g_osi_funcs_p + 0x194` as `wifi_osi_funcs_t._coex_status_get` on ESP32. Generated `esp-wifi-sys` bindings and the compiled `esp_radio::wifi::os_adapter::coex_status_get` function confirm the current no-`coex` image returns constant `0`, so this callback is not a credible mixed branch source for the retry1 `g_chm` scalar split. | diagnostic outcome / root-cause boundary corrected | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md) |
| same scan caller explains both retry1 `g_chm +0x08/+0x0c` states | Treat both retry1 active-op scalar pairs as outcomes of the same `clear_bss_queue -> chm_start_op` call site. Static disassembly shows the scalar pair stored into `g_chm` comes from scan-side dwell/channel metadata selected before the `chm_start_op` call, and `scan_inter_channel_timeout_process` feeds back into the same path. This explains the live progression from `ptr_08=0x0 ptr_0c=0x78` to `ptr_08=0x0a ptr_0c=0x14` without requiring a second direct `chm_start_op` caller or a nonzero `coex_status_get` branch. | diagnostic outcome / root-cause boundary tightened | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [blob-state boot-scan log](../../logs/boot_scan_only_diag_blobstate_20260306_berlin_retry1/boot_espflash_monitor.log) |
| `esp-radio` IDF-like default broad-scan timing (`MEDITAMER_WIFI_ESP_RADIO_USE_IDF_DEFAULT_SCAN_TIMING_DIAG`) | Keep default `off`; diagnostic-only. Forcing the wrapper default broad scan to IDF-like timing (`active 0/120`, `home dwell 30`) materially changes the internal no-std `g_chm` path by flattening the earlier visible active-op progression, but both the direct IDF null-config compare and the wrapped Rust scan still return zero APs. Treat this as causal evidence that timing values affect the path shape, but not as a fix. | diagnostic outcome / insufficient-cause proven / keep default off | 2026-03-06 | [History part-16](./upload-throughput-history/part-16.md), [IDF-scan-timing boot-scan log](../../logs/boot_scan_only_diag_idfscantiming_20260306_berlin.log) |
| boot-only direct IDF explicit broad scan compare (`MEDITAMER_WIFI_BOOT_SCAN_ONLY_DIAG_IDF_EXPLICIT_COMPARE`) | Keep default `off`; diagnostic-only. In the same failing no-std image, direct IDF `NULL` scan, direct IDF explicit broad scan, and wrapped `esp-radio` broad scan all fail identically with `scan_done count=0` / `ap_num=0` / `result_count=0`, while the flattened `g_chm` shape remains unchanged across all three. This closes `NULL` vs explicit scan config and wrapper-vs-direct scan-call selection as primary causes. | diagnostic outcome / insufficient-cause proven / keep default off | 2026-03-07 | [History part-16](./upload-throughput-history/part-16.md), [IDF-explicit boot-scan log](../../logs/boot_scan_only_diag_idfexplicit_esptool_20260307_berlin.log) |
| no-std dominant timer execution lateness | Keep diagnostic retained: the dominant `nan_dp_schedule_ndc_start(arg=1)` timer family executes effectively on time in the bounded failing run (`last_timeout_us=120000`, `last_lateness_us≈212-249 us`, `max_lateness_us=305 us`), while all three scan entry variants still return zero APs. This rejects gross no-std timer/scheduler lateness as the primary cause. | diagnostic outcome / insufficient-cause proven | 2026-03-07 | [History part-16](./upload-throughput-history/part-16.md), [timer-lateness boot-scan log](../../logs/boot_scan_only_diag_timerlateness_20260307_berlin.log) |
| no-std `g_scan` / `connect_scan_flag` / `app_scan_params` stability across failing scans | Keep diagnostic retained: with the timing-normalized no-std image, `g_scan`, `connect_scan_flag`, and `app_scan_params` remain materially flat across direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust broad scans, while all three still end with `scan_done count=0`. The only visible progression in this slice is `g_scan + 0x114` incrementing (`0x80 -> 0x81 -> 0x82`), which is not sufficient to restore visibility. Treat visible scan-parameter object mutation as an insufficient cause and keep the target below scan-state objects. | diagnostic outcome / insufficient-cause proven | 2026-03-07 | [History part-16](./upload-throughput-history/part-16.md), [scan-globals boot-scan log](../../logs/boot_scan_only_diag_scanglobals_20260307_berlin/boot_espflash_monitor.log) |
| additional no-std `scan_inter_channel_timeout_process` gate-field stability | Keep diagnostic retained: in the same timing-normalized no-std image, the additional readily inspectable gate inputs from `g_wifi_nvs`, `sta`, and `g_scan` also remain flat across direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust broad scans: `wifi_nvs_byte_35c=0x18`, `sta.word_0e4=0`, `g_scan.byte_119=0`, `g_scan.ptr_194=0xffffffff`, while all three still end with `scan_done count=0`. Treat visible mutation in these gate fields as an insufficient cause and keep the target below these probeable gate bytes. | diagnostic outcome / insufficient-cause proven | 2026-03-07 | [History part-16](./upload-throughput-history/part-16.md), [scan-gates boot-scan log](../../logs/boot_scan_only_diag_scangates_20260307_berlin/boot_espflash_monitor.log) |
| raw `ScanDone` BSS-list probe (`scannum`, `g_ic + 0x130/+0x134`) | Keep diagnostic retained: in the timing-normalized no-std image, the raw `ScanDone` event already sees an empty result list for all three failing scan variants: `scannum=0`, `head_ptr=0`, stable sentinel `tail_ptr=0x3ffd32b0`. This proves the failure is not “records were populated and then cleared before `get_ap_num`”; the BSS/result list is empty at the event boundary. | diagnostic outcome / root-cause boundary tightened | 2026-03-07 | [History part-16](./upload-throughput-history/part-16.md), [scan-done-list boot-scan log](../../logs/boot_scan_only_diag_scandonelist_20260307_berlin/boot_espflash_monitor.log) |
| no-std Wi-Fi allocator activity during failing scans | Keep diagnostic retained: the timing-normalized no-std image exercises Wi-Fi allocation hooks during all three failing scan variants (`malloc_internal`, `wifi_malloc`, `wifi_calloc`, `free` all advance materially), yet the raw `ScanDone` event still reports `scannum=0` and `head_ptr=0`. This proves the scan path is not dead before all memory activity, and tightens the remaining target to candidate rejection/free-before-link or adjacent scan machinery that never reaches BSS-list insertion. | diagnostic outcome / root-cause boundary tightened | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [alloc boot-scan log](../../logs/boot_scan_only_diag_alloc_20260309_berlin/boot_espflash_monitor.log) |
| fixed `g_cnxMgr` BSS-pool slots during failing scans | Keep diagnostic retained: in the env-gated boot-scan image, all four fixed `g_cnxMgr` slots walked by `cnx_bss_alloc` remain completely blank across direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust broad scans (`bssid=00:00:00:00:00:00`, `word_0c=0`, `word_2a8=0`, `word_2ac=0`), while raw `ScanDone` still reports `scannum=0` and `head_ptr=0`. This proves the failing no-std scan never reaches fixed-pool BSS admission, and pushes the root-cause target earlier into `check_bss_queue` / `scan_parse_beacon` reject paths before `cnx_bss_alloc`. | diagnostic outcome / root-cause boundary tightened | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [cnx-slot boot-scan log](../../logs/boot_scan_only_diag_cnxslots_20260309_envrun/boot_espflash_monitor.log) |
| `g_scan` history-table state during failing scans | Keep diagnostic retained: in the env-gated boot-scan image, the `g_scan` history count at `+0x110` stays `0`, and the first three rows at `+0x0a4/+0x0c4/+0x0e4` remain zero-filled with zero metadata across direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust broad scans. Together with the empty `g_cnxMgr` pool and empty raw `ScanDone` list, this proves the failing no-std path is earlier than `scan_update_scan_history` and earlier than fixed-pool BSS admission. | diagnostic outcome / root-cause boundary tightened | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [scan-history boot-scan log](../../logs/boot_scan_only_diag_scanhistory_20260309_envrun/boot_espflash_monitor.log) |
| exact boot-scan-window promisc visibility on the failing no-std path | Keep diagnostic retained: in the env-gated boot-scan image, the pre-scan promiscuous sweep sees zero packets of any type on channels `8/1/6/11` (`total=0 mgmt=0 ctrl=0 data=0 misc=0`), and the same run then returns zero results for direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust scans. Together with the empty history table, empty `g_cnxMgr` pool, and empty raw `ScanDone` list, this pins the remaining target to the earliest RX/scan ingress path rather than later beacon parsing or result bookkeeping. | diagnostic outcome / root-cause boundary tightened | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [promisc-window boot-scan log](../../logs/boot_scan_only_diag_promiscwindow_20260309_envrun/boot_espflash_monitor.log) |
| real upstream-style PHY common clock refcounting in vendored `esp-radio` | Reject as primary fix. With `MEDITAMER_WIFI_PHY_COMMON_CLOCK_ENABLE_REAL_DIAG=1`, the no-std blob actively calls the hook hundreds of times (`enable_calls=236 disable_calls=210 ref_count=26` by `rust_scan`), but the pre-scan promiscuous sweep is still all-zero and all three scan variants still return zero APs. This closes the “no-op `phy_common_clock_enable()` is the primary cause” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [phy-common-clock boot-scan log](../../logs/boot_scan_only_diag_phyclockreal_20260309_090054/boot_espflash_monitor.log) |
| eager precreate of the `esp-rtos` timer task before Wi-Fi init | Reject as primary fix. With `MEDITAMER_WIFI_PRECREATE_TIMER_TASK_DIAG=1`, the `timer` task is precreated before `esp_radio::wifi::new()`, but the exact boot-scan promisc sweep stays all-zero and the direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust scans still all return zero APs. This closes the “lazy timer task creation races the first `chm_init` window” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [precreate-timer boot-scan log](../../logs/boot_scan_only_diag_precreatetimer_20260309_090917/boot_espflash_monitor.log) |
| boot-scan-only Wi-Fi RF hold (`MEDITAMER_WIFI_BOOT_SCAN_ONLY_FORCE_WAKEUP_DIAG`) | Reject as primary fix. With `MEDITAMER_WIFI_BOOT_SCAN_ONLY_FORCE_WAKEUP_DIAG=1`, `esp_wifi_force_wakeup_acquire()` and `...release()` both succeed (`rc=0`), but the exact pre-scan promisc window stays all-zero and direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust scans still all return zero APs with empty raw `ScanDone` lists. This closes the “radio is simply sleeping / RF closed during the no-std boot scan” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [force-wakeup boot-scan log](../../logs/boot_scan_only_diag_forcewakeup_20260309_092120/boot_espflash_monitor.log) |
| force `wifi_task_core_id` to the IDF default core (`MEDITAMER_WIFI_ESP_RADIO_USE_IDF_TASK_CORE`) | Reject as primary fix. With `MEDITAMER_WIFI_ESP_RADIO_USE_IDF_TASK_CORE=1`, the exact boot-scan promisc window stays all-zero and direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust scans still all return zero APs with empty raw `ScanDone` lists. This closes the “Wi-Fi task core placement is the primary cause” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [IDF-task-core boot-scan log](../../logs/boot_scan_only_diag_idftaskcore_20260309_092559/boot_espflash_monitor.log) |
| boot-scan-only extra `phy_enable()` reference (`MEDITAMER_WIFI_BOOT_SCAN_ONLY_FORCE_PHY_ENABLE_DIAG`) | Reject as primary fix. With `MEDITAMER_WIFI_BOOT_SCAN_ONLY_FORCE_PHY_ENABLE_DIAG=1`, the boot-scan path invokes one extra `phy_enable()` reference and the adapter counters confirm `phy_enable_count=2` by the first direct IDF compare, yet the pre-scan promisc window stays all-zero and direct IDF `NULL`, direct IDF explicit broad, and wrapped Rust scans still all return zero APs with empty raw `ScanDone` lists. This closes the “missing PHY enable reference is the primary cause” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [force-phy boot-scan log](../../logs/boot_scan_only_diag_forcephy_20260309_093237/boot_espflash_monitor.log) |
| standalone legacy no-std `esp-wifi 0.15.1` comparator | Keep as primary boundary evidence. A standalone published-crate no-std `esp-wifi 0.15.1` tool built with the legacy `xtensa-esp32-elf-gcc` linker and flashed app-only to `0x10000` scans successfully on the same board/network (`scan=ok count=3`, expected APs present). This proves the board, environment, and no-std execution model can scan successfully, and moves the primary regression boundary up to the current `esp-radio` / `esp-rtos` stack used by the main firmware and the standalone `esp_radio_nostd_wifi_control` probe. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [legacy no-std esp-wifi comparator log](../../logs/esp_wifi_legacy_nostd_control_20260309_095109/monitor.log) |
| standalone current `esp-radio` comparator with explicit `esp-rtos` yields | Reject as sufficient fix. In the standalone current-stack comparator, explicit `esp_rtos::yield_for_esp_radio_diag()` handoffs after `esp_rtos::start`, `esp_radio::init`, `wifi_new`, and `wifi_start` all run, yet the probe still ends with `scan=ok count=0`. This closes the narrow “current stack only needs an explicit one-time scheduler handoff/yield” branch and keeps the remaining boundary in deeper `esp-radio` / `esp-rtos` runtime semantics. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-18](./upload-throughput-history/part-18.md), [current no-std esp-radio yield comparator log](../../logs/esp_radio_nostd_wifi_control_yielddiag_20260309_095543/monitor.log) |
| current `esp-rtos` legacy-style poll-and-yield waits | Reject as fix; keep as strong runtime-boundary evidence. With `MEDITAMER_WIFI_ESP_RTOS_LEGACY_POLL_WAIT_DIAG=1`, blocking semaphore and queue waits in vendored `esp-rtos` switch to legacy-style poll-and-yield loops, but the standalone current `esp-radio` comparator regresses further and stalls after `esp_radio_init=ok`, before `wifi_new`, with no later serial progress. This proves the working legacy built-in scheduler is not equivalent to simply polling/yielding everywhere, and keeps the remaining boundary in more specific `esp-rtos` task/timer/queue semantics immediately after `esp_radio::init`. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio legacy-poll comparator log](../../logs/esp_radio_nostd_wifi_control_legacypoll_20260309_100205/monitor.log) |
| current standalone `esp-radio` task/queue creation boundary | Keep as runtime-boundary evidence. In the standalone current-stack comparator, `esp_radio_init` creates no runtime task/queue objects, while `wifi_new` creates exactly one task and one queue (`capacity=200`, `item_size=8`), and `wifi_start` adds nothing further. The scan still ends at `count=0`. This moves the remaining target from create-time into run-time behavior of that first queue/task pair, not runtime object creation itself. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio creation-diag log](../../logs/esp_radio_nostd_wifi_control_creatediag_20260309_101042/monitor.log) |
| current standalone `esp-radio` queue runtime traffic boundary | Keep as runtime-boundary evidence. In the standalone current-stack comparator, the single queue created by `wifi_new` stays active on the failing path: after `wifi_start` it already shows balanced send/recv traffic on the same queue pointer, and during the failing scan it carries heavy traffic (`queue_send=41`, `queue_send_isr=14`, `queue_recv=55`) on the same dominant queue while the scan still ends at `count=0`. This closes the “queue never runs” branch and moves the remaining target to the semantics of what that queue/task pair is doing during scan, not queue creation or basic scheduling. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio queue-traffic log](../../logs/esp_radio_nostd_wifi_control_queuetraffic_20260309_101640/monitor.log) |
| current standalone `esp-radio` queue task-role pattern | Keep as runtime-boundary evidence. In the isolated standalone current comparator, the same dominant queue-role/message-family pattern seen in the main firmware branch reproduces directly: after `wifi_start` the queue is `main -> wifi`, and during the failing scan it adds `wifi -> wifi` message families (`0x0`, `0x10`) plus `timer -> wifi` `0x7/0x8/0x0` traffic on the same queue, yet the scan still ends at `count=0`. This makes the role/message-family pattern a property of the current `esp-radio` stack itself, not an artifact of the larger firmware/harness, and tightens the remaining target to semantic handling of that active queue traffic. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio task-role log](../../logs/esp_radio_nostd_wifi_control_taskroles_20260309_102156/monitor.log) |
| current standalone `esp-radio` queue consumer-side item progression | Keep as runtime-boundary evidence. In the isolated standalone current comparator, the `wifi` task actively dequeues semantic items during the failing scan: after `wifi_start` the consumer receives the same `0x6` control items that `main` sends, and during the zero-result scan it consumes a richer family including `0x0`, `0x10`, timer-originated `0x7/0x8/0x0`, and a newly surfaced consumer-side `0x17` family. This closes the “queue traffic exists but may never be consumed” branch and makes consumer dispatch semantics, especially the meaning of receive-side `0x17`, the next high-value target. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio receive-item log](../../logs/esp_radio_nostd_wifi_control_recvitems_20260309_102546/monitor.log) |
| current standalone `esp-radio` sender-recent vs consumer-side mismatch around `0x17` | Keep as runtime-boundary evidence. In the isolated standalone current comparator, the latest sender-side ordinals near the failing scan tail still only show `0x6`, `0x0`, `0x10`, and timer `0x7/0x8/0x0`, while the consumer-side receive samples in the same failing scan still include `0x17`. This keeps the next target narrow: trace receive ordering/dispatch around `0x17`, because the interesting mismatch is now inside consumer-side progression rather than generic queue activity. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio sender-recent log](../../logs/esp_radio_nostd_wifi_control_sendrecent_20260309_102821/monitor.log) |
| current standalone `esp-radio` recent receive-side `0x17` tail event | Keep as primary runtime-boundary evidence. In the isolated standalone current comparator, the recent receive ring confirms `0x17` as a stable late consumer-side event in the failing scan tail: ordinals `49..54` on the `wifi` task include `0x7`, `0x0`, `0x17`, `0x10`, `0x7`, and `0x6`, while the latest sender-side ordinals in the same tail still do not expose `0x17`. This makes consumer-side handling/dispatch of `0x17` the next precise target. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-19](./upload-throughput-history/part-19.md), [current no-std esp-radio receive-recent log](../../logs/esp_radio_nostd_wifi_control_recvrecent_20260309_103120/monitor.log) |
| working legacy no-std scan also carries late `0x17` / `0x10` / `0x7` queue traffic | Reject as unique regression signature. The working standalone legacy no-std `esp-wifi 0.15.1` comparator scans successfully (`count=4`) and its `after_scan` queue tail already contains `0x17`, `0x10`, timer `0x7/0x8/0x0`, and trailing `0x6` control items. This proves late `0x17` / TxComplete queue traffic is not unique to the broken current stack and moves the root-cause focus back earlier, to why the current stack reaches zero pre-scan promisc / zero admission while the legacy stack still forms AP records under broadly similar queue-tail families. | diagnostic outcome / rejected unique-signature hypothesis | 2026-03-09 | [History part-20](./upload-throughput-history/part-20.md), [legacy no-std queue-diag monitor log](../../logs/esp_wifi_legacy_nostd_control_queuediag_steady2_20260309_105433/espflash_clean_monitor.log) |
| working legacy no-std pre-scan promisc window | Keep as the tightest same-board discriminator. The working standalone legacy no-std `esp-wifi 0.15.1` comparator now shows non-zero pre-scan promiscuous visibility on channels `8/1/6/11` (`total=16`, `mgmt=11`, `data=5`) and then immediately scans successfully (`count=6`) on the same board/network. This proves the board and no-std execution model can see management/data frames before scan admission, while the current `esp-radio` stack remains completely dark in the same window. Root-cause focus should stay on earliest RX ingress / frame-admission behavior in the current `esp-radio` / `esp-rtos` stack. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-20](./upload-throughput-history/part-20.md), [legacy no-std promisc summary](../../logs/esp_wifi_legacy_nostd_control_promisc_20260309_berlin/monitor_summary.log) |
| isolated current standalone `esp-radio` pre-scan promisc window | Keep as the tightest current-stack boundary evidence. The isolated standalone current `esp-radio` comparator now reproduces the same dark pre-scan window seen in the larger firmware: channels `8/1/6/11` all remain `total=0 mgmt=0 ctrl=0 data=0 misc=0`, aggregate remains zero, and the subsequent scan still returns `count=0`. This proves the zero-ingress boundary is intrinsic to the current `esp-radio` / `esp-rtos` stack itself, not to the larger firmware harness. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-20](./upload-throughput-history/part-20.md), [current standalone promisc summary](../../logs/esp_radio_nostd_wifi_control_promisc_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with `esp-rtos` `opt-level = 3` | Reject as sufficient fix. Promoting `esp-rtos` alone to `opt-level = 3` in the standalone current comparator preserves clean startup and the same single task/queue creation after `wifi_new`, but scan still ends at `count=0`. This closes the “unoptimized `esp-rtos` dev profile is the blackout cause” branch; the remaining target stays in current `esp-radio` / `esp-rtos` RX-ingress semantics rather than generic runtime optimization level. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-20](./upload-throughput-history/part-20.md), [current standalone `esp-rtos` opt3 summary](../../logs/esp_radio_nostd_wifi_control_esp_rtos_opt3_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with legacy-style simple semaphores | Reject as sufficient fix. Replacing current semaphore create/take/give/delete behavior with a legacy-style simple counting semaphore in the standalone current comparator still preserves clean startup, the same single task/queue creation after `wifi_new`, and still ends at `scan=ok count=0`. This closes the “current handle-based semaphore semantics are the primary cause” branch while keeping queue/runtime behavior otherwise unchanged. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-20](./upload-throughput-history/part-20.md), [current standalone legacy-simple-sem summary](../../logs/esp_radio_nostd_wifi_control_legacysimplesem_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with legacy-style simple queues | Reject as sufficient fix. Replacing current queue create/send/receive/delete behavior with a legacy-style simple queue in the standalone current comparator still preserves clean startup, the same single task/queue creation after `wifi_new`, the same late `0x6` / `0x0` / `0x10` / `0x7` / `0x17` queue-family tail, and still ends at `scan=ok count=0`. This closes the “current `QueueHandle` / `QueuePtr` semantics are the primary cause” branch while keeping the regression boundary earlier in RX ingress / scan admission. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-20](./upload-throughput-history/part-20.md), [current standalone legacy-simple-queue summary](../../logs/esp_radio_nostd_wifi_control_legacysimplequeue_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with legacy-like ESP32 PHY wrapper | Reject as sufficient fix. Restoring a guarded legacy-like ESP32 PHY enable/disable wrapper around the current `esp-radio` PHY entry points still preserves clean startup, the same single task/queue creation after `wifi_new`, and still ends at `scan=ok count=0` with the same late queue-family tail. This closes the “current simplified ESP32 PHY wrapper is the primary cause” branch and keeps the regression boundary earlier in RX ingress / scan admission. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-21](./upload-throughput-history/part-21.md), [current standalone legacy-PHY summary](../../logs/esp_radio_nostd_wifi_control_legacyphy_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with legacy-style simple queues and semaphores together | Reject as sufficient fix. Enabling both legacy-style simple semaphores and legacy-style simple queues together in the isolated current standalone comparator still preserves clean startup, the same single task/queue creation after `wifi_new`, and still ends at `scan=ok count=0` with the same late queue-family tail. This closes the “queue/semaphore interaction is the missing runtime contract” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-21](./upload-throughput-history/part-21.md), [current standalone legacy-simple-both summary](../../logs/esp_radio_nostd_wifi_control_legacysimpleboth_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with timer task precreated before `esp_radio::init()` | Reject as sufficient fix. A valid clean standalone run that definitely precreated the current `esp-rtos` timer task before `esp_radio::init()` still preserves clean startup, keeps runtime object creation starting only at `wifi_new`, leaves the full pre-scan promisc window dark on channels `8/1/6/11`, and still ends at `scan=ok count=0`. This closes the “timer task exists too late” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-21](./upload-throughput-history/part-21.md), [current standalone precreate-timer summary](../../logs/esp_radio_nostd_wifi_control_precreatetimer_clean_20260309_berlin/monitor_summary.log) |
| legacy vs current no-std task/thread-sem identity | Treat stable thread-semaphore identity as non-discriminating: both the working legacy no-std stack and the failing current `esp-radio` stack keep thread-semaphore pointer and thread-semaphore task identity stable through startup and scan. Also reject “any `task_get_current_task` churn” as unique to failure: legacy still scans successfully while reaching `task_get_current_change_count=2`; current fails with the same stable thread-sem behavior but a larger churn count (`4`). The remaining target is the meaning of those extra current-stack current-task transitions, not thread-sem instability or the mere presence of task churn. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-21](./upload-throughput-history/part-21.md), [legacy task/sem monitor](../../logs/esp_wifi_legacy_nostd_control_tasksem2_20260309_berlin/monitor.log), [current task/sem monitor](../../logs/esp_radio_nostd_wifi_control_tasksem_20260309_berlin/monitor.log) |
| legacy vs current no-std recent `task_get_current_task` rings | Treat task-role switching as non-discriminating in the recent sampled window: both the working legacy no-std stack and the failing current `esp-radio` stack report recent `task_get_current_task` rings that stay pinned to `main` after `wifi_new`, after `wifi_start`, and after scan. That means the extra current-stack churn count is not explained by visible sampled transitions to `wifi` or `timer`; the remaining target lies below simple task-role changes captured by the recent ring. | diagnostic outcome / primary boundary tightened | 2026-03-09 | [History part-21](./upload-throughput-history/part-21.md), [legacy task-recent monitor](../../logs/esp_wifi_legacy_nostd_control_taskrecent_20260309_berlin/monitor.log), [current task-recent monitor](../../logs/esp_radio_nostd_wifi_control_taskrecent_build_20260309_berlin/monitor.log) |
| standalone current `esp-radio` with legacy-style Wi-Fi/internal allocation | Reject as sufficient fix. Forcing current `esp-radio` `malloc_internal()` and `calloc_internal_wrapper()` to use legacy-style generic heap allocation, which also makes `wifi_malloc()` / `wifi_calloc()` follow legacy heap semantics, still preserves clean startup, leaves the pre-scan promisc window fully dark on channels `8/1/6/11`, and still ends at `scan=ok count=0` in the isolated current standalone comparator. This closes the “current internal-only Wi-Fi allocation semantics are the primary cause” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-21](./upload-throughput-history/part-21.md), [current standalone legacy-allocation summary](../../logs/esp_radio_nostd_wifi_control_legacyalloc_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with legacy `phy_mem_init()` at top-level init | Reject as sufficient fix. Adding the legacy ESP32 `phy_mem_init()` step to current `esp-radio::init()` before ISR setup still preserves clean startup, leaves the pre-scan promisc window fully dark on channels `8/1/6/11`, and still ends at `scan=ok count=0` in the isolated current standalone comparator. This closes the “missing legacy `phy_mem_init()` in current top-level init is the primary cause” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [current standalone legacy-phy-mem-init summary](../../logs/esp_radio_nostd_wifi_control_legacymphyinit_20260309_berlin/monitor_summary.log) |
| standalone current `esp-rtos` with legacy-style timer-loop wake policy | Reject as viable fix candidate. Replacing current `esp-rtos` timer-queue `sleep_until(next_wakeup)` behavior with a legacy-style `yield_task()` loop regresses the isolated current standalone comparator earlier than Wi-Fi bring-up: boot output stops immediately after the first `after_rtos_start` checkpoint and never reaches `precreate_timer_task`, `esp_radio_init`, or `wifi_new`. This closes the “make current timer-loop semantics match legacy timer task directly” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [current standalone legacy-timer-loop summary](../../logs/esp_radio_nostd_wifi_control_legacytimerloop_20260309_berlin/monitor_summary.log) |
| isolated legacy vs current no-std `wifi_init_config_t` snapshots | Treat init-config shape as non-discriminating. The working legacy `esp-wifi 0.15.1` standalone and the failing current `esp-radio` standalone now show matching effective `wifi_init_config_t` field values at Wi-Fi init handoff (`static_rx_buf_num=10`, `dynamic_rx_buf_num=32`, `rx_mgmt_buf_num=5`, `rx_ba_win=6`, `nvs_enable=0`, `nano_enable=0`, `wifi_task_core_id=0`, `feature_caps=0x81`, `magic=0x1f2f3f4f`) and both populate all critical OSI slots (`_set_isr`, `_queue_create`, `_queue_recv`, `_task_create`, `_task_create_pinned_to_core`, `_task_get_current_task`, `_wifi_thread_semphr_get`, `_timer_arm_us`, `_event_post`, `_malloc_internal`). Legacy still sees pre-scan packets and scans, current still sees none and scans zero. This closes the “effective init-config field values or missing OSI slot pointers are the primary cause” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [current init-config summary](../../logs/esp_radio_nostd_wifi_control_initdiag_20260309_berlin/monitor_summary.log), [legacy init-config summary](../../logs/esp_wifi_legacy_nostd_control_initdiag_20260309_berlin/monitor_summary.log) |
| standalone current `esp-radio` with legacy-style `task_delay()` | Reject as sufficient fix. Replacing current `task_delay()` sleep semantics with a legacy-style microsecond deadline loop that repeatedly `yield_task()` still preserves clean startup, leaves the pre-scan promisc window fully dark on channels `8/1/6/11`, and still ends at `scan=ok count=0` in the isolated current standalone comparator. This closes the “legacy-style `task_delay()` restores early RX ingress” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [current standalone legacy-task-delay summary](../../logs/esp_radio_nostd_wifi_control_legacytaskdelay_20260309_berlin/monitor_summary.log) |
| isolated legacy vs current no-std `wifi` task creation, plus forced current max priority | Treat simple Wi-Fi task creation shape and max-priority override as non-discriminating. Both isolated stacks create exactly one `wifi` task after `wifi_new` with matching name (`wifi`), stack depth (`6656`), and core (`0`). Legacy requests `prio=253` and still sees pre-scan packets and scans; current requests `prio=29` and stays dark. Forcing the current runtime to raise that task to effective max priority (`task_create_last_effective_priority=31`) still leaves pre-scan promisc fully dark and scan at `count=0`. This closes the “current Wi-Fi task priority is the primary cause” branch. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [legacy task-create monitor](../../logs/esp_wifi_legacy_nostd_control_taskcreate_20260309_berlin/monitor.log), [current task-create monitor](../../logs/esp_radio_nostd_wifi_control_taskcreate_20260309_berlin/monitor.log), [current forced-priority monitor](../../logs/esp_radio_nostd_wifi_control_forcewifiprio2_20260309_berlin/monitor.log) |
| isolated legacy vs current no-std `WIFI_MAC` ISR counts | Treat dead `WIFI_MAC` interrupts as rejected. The working legacy no-std stack and the failing current no-std stack both show zero ISR count before scan and nonzero ISR count by the end of scan (`58` legacy, `35` current). Legacy still sees pre-scan management/data packets and scans successfully; current still sees zero pre-scan packets and scans zero. This closes the “current isolated stack never gets `WIFI_MAC` interrupts” branch and keeps the boundary after ISR delivery and before early RX admission. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [legacy ISR monitor](../../logs/esp_wifi_legacy_nostd_control_isrdiag_20260309_berlin/monitor.log), [current ISR monitor](../../logs/esp_radio_nostd_wifi_control_isrdiag_20260309_berlin/monitor.log) |
| legacy isolated `esp-wifi 0.15.1` without `builtin-scheduler` | Treat the `builtin-scheduler` feature flag itself as non-discriminating. A cloned legacy standalone tool with only `builtin-scheduler` removed still starts cleanly, sees pre-scan promisc traffic (`total=11`), completes scan successfully (`count=4`), and still shows live `WIFI_MAC` ISR activity (`45`). This closes the “current fails simply because it lacks the legacy `builtin-scheduler` feature” branch and shifts the remaining boundary to the newer stack/runtime combination (`esp-radio 0.17.0` + `esp-wifi-sys 0.8.1` + `esp-hal 1.0.0` + `esp-rtos`). | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [legacy no-builtin monitor](../../logs/esp_wifi_legacy_nostd_control_nobuiltin_20260309_berlin/monitor.log) |
| isolated `esp-radio 0.16.0` + `esp-rtos 0.1.0` + `esp-wifi-sys 0.8.1` | Treat the failure as broader than `esp-radio 0.17.0`. A minimal public-API-only standalone tool pinned to the coherent `0.16.0` generation still starts cleanly, but its pre-scan promisc window is fully dark and scan still returns `count=0`. This closes the “blackout is unique to `esp-radio 0.17.0`” branch and shifts the remaining boundary to the newer no-std generation shared by `esp-radio 0.16.x+`, `esp-wifi-sys 0.8.1`, and `esp-rtos`. | diagnostic outcome / rejected variant | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md), [radio-0.16 standalone monitor](../../logs/esp_radio_016_nostd_wifi_control_20260309_berlin/monitor.log) |
| patched `esp-wifi 0.15.1` onto `esp-wifi-sys 0.8.1` | Treat the sys-generation boundary as structurally significant. A strict path-override experiment that moved the working legacy `esp-wifi 0.15.1` wrapper onto `esp-wifi-sys 0.8.1` did not reach runtime; it failed at compile time with broad ABI/API drift across NAN/NDP event wrappers, `wpa_crypto_funcs_t`, `wifi_scan_config_t`, `wifi_ap_config_t`, and `wifi_scan_threshold_t`. This is not a narrow compatibility fix. It strengthens the current boundary between the working `0.7.1` generation and the failing newer `0.8.1` generation. | diagnostic outcome / rejected simple port | 2026-03-09 | [History part-22](./upload-throughput-history/part-22.md) |
| firmware Wi-Fi backend seam with current `esp-radio` kept active | Keep this seam and migrate through it. The firmware upload Wi-Fi path now routes runtime init through `src/firmware/storage/upload/wifi/backend.rs`, preserves the original controller/device type shape, logs the selected backend as `esp-radio`, keeps backend selection explicit in Cargo through `wifi-backend-esp-radio`, funnels upload-path controller operations through backend-owned wrapper functions instead of direct `esp-radio` methods, and now also hides backend-specific runtime-config construction and `NoMem` error classification behind backend-owned helpers. A direct optional `esp-wifi 0.15.1` dependency in the main crate is currently blocked by an `xtensa-lx-rt` links conflict, so the real legacy backend will need either a separate crate boundary or a vendored/ported backend layer. | architectural step / migration path enabled | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md) |
| runtime init/reinit moved behind firmware Wi‑Fi seam | Keep this refactor and build on it. `UploadHttpRuntime::setup()` no longer owns raw `StaticCell<RadioController>`, direct `init_radio()`, direct `new_runtime(...)`, or setup-reinit sequencing; those now live in `src/firmware/storage/upload/wifi/runtime_init.rs` and are exported through the Wi‑Fi layer. Main setup only asks for `(WifiController<'static>, WifiDevice<'static>)` and then builds `embassy_net`. This removes another backend-specific surface before porting a legacy-style backend. | architectural step / migration path enabled | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md) |
| setup-time storage override and early driver-state logging moved behind Wi‑Fi seam | Keep this refactor and build on it. `UploadHttpRuntime::setup()` no longer owns raw `esp_wifi_sys` storage override or early runtime driver-state logging; those now live behind `apply_runtime_setup_overrides_and_log()` in `src/firmware/storage/upload/wifi/runtime_init.rs`. Main upload setup is now reduced to orchestration and `embassy_net` assembly rather than direct Wi‑Fi driver introspection. | architectural step / migration path enabled | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md) |
| client-mode and scan-config builders moved behind Wi‑Fi seam | Keep this refactor and build on it. Backend-owned helpers in `src/firmware/storage/upload/wifi/backend.rs` now construct STA mode config, active/directed/channel/passive/raw scan configs, standard B/G/BGN protocols, STA mode, and power-save-none. This removes another class of `esp-radio`-specific builder and enum assumptions from the upload state machine and narrows the remaining work for a legacy backend implementation. | architectural step / migration path enabled | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md) |
| dead optional legacy-backend feature stub removed | Keep this cleanup. The unused `wifi-backend-esp-wifi-legacy` Cargo feature stub and its matching unreachable `compile_error!()` branch were removed so the codebase no longer advertises a dormant optional-dependency path that is not part of the actual migration strategy. The remaining path is explicit: port legacy behavior source-level behind the existing backend seam. | architectural step / migration path clarified | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md) |
| `backend_legacy_port` staging module created in-tree | Keep this staging area and build the real port through it. The firmware tree now contains a compile-clean `src/firmware/storage/upload/wifi/backend_legacy_port/` module that encodes the proven legacy bootstrap sequence, scheduler contract, effective init-config invariants, Wi‑Fi task contract, and scope of the first real migration increment (`init/start/scan/stop` first; connect/device path deferred). This replaces ad hoc notes with an explicit in-tree target for the source-level legacy backend port. | architectural step / migration path enabled | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md) |
| legacy-port hook availability encoded in-tree | Keep this blocker inventory. `src/firmware/storage/upload/wifi/backend_legacy_port/availability.rs` now makes the migration boundary explicit: current stack equivalents exist for `enable_wifi_power_domain`, `phy_mem_init`, `setup_radio_isr`, `wifi_set_log_verbose`, `init_radio_clocks`, and `coex_initialize`, but the legacy scheduler bootstrap steps `preempt::enable`, `init_tasks`, and explicit `initial_yield` still have no direct current-stack equivalent at the firmware layer. This means the next real port step must move below the firmware backend seam. | architectural step / blocker clarified | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md) |
| `backend_legacy_port` owns controller `start/scan/stop` semantics | Keep and build on it. `src/firmware/storage/upload/wifi/backend_legacy_port/controller.rs` now owns the legacy-port controller `start/scan/stop` behavior, and generic `src/firmware/storage/upload/wifi/backend.rs` delegates to it when the legacy-port path is active. This keeps both runtime bootstrap/init and controller semantics inside the same source-level port target instead of scattering them across generic backend glue. | architectural step / migration path enabled | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md) |
| `backend_legacy_port` owns runtime config, direct init path, and local Wi-Fi types | Keep and build on it. The legacy-port module now owns its own runtime config builder, direct `esp_radio::init()` / `esp_radio::wifi::new(...)` path, and local type aliases for controller/device/error/scan types, instead of receiving those through generic backend wrappers. This makes the source-level port materially deeper and keeps the remaining blocker focused on missing legacy runtime behavior rather than module ownership. | architectural step / migration path enabled | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md) |
| `esp-rtos` legacy bootstrap shim exposed through `backend_legacy_port` | Keep this as a lower-layer migration aid, not as “legacy backend works”. The vendored `esp-rtos` layer now exposes `bootstrap_legacy_wifi_contract_shim()`, and `src/firmware/storage/upload/wifi/backend_legacy_port/bootstrap.rs` maps it into a firmware-local `LegacyBootstrapRuntimeStatus`. This gives the legacy-port path one executable approximation of the missing scheduler bootstrap contract (`scheduler initialized`, timer task precreated, one initial yield) while still leaving the real blockers unresolved: there is still no direct current equivalent for legacy `preempt::enable()` or full `init_tasks()` semantics. | architectural step / blocker clarified | 2026-03-09 | [History part-24](./upload-throughput-history/part-24.md) |
| `backend_legacy_port` executable runtime/controller path | Keep and build on it. `src/firmware/storage/upload/wifi/backend_legacy_port/runtime.rs` now makes the legacy-port path executable in firmware instead of staging-only: with `MEDITAMER_WIFI_BACKEND_LEGACY_PORT_DIAG=1`, runtime init routes through `backend_legacy_port::initialize_runtime_sta_legacy_port(...)`, logs the legacy bootstrap contract plus current shim status, and the same knob now also governs legacy-style synchronous controller `start/scan/stop` behavior through the backend seam. This is the first coherent end-to-end firmware selection path for the legacy-runtime/backend port. | architectural step / migration path enabled | 2026-03-09 | [History part-24](./upload-throughput-history/part-24.md) |
| `backend_legacy_port` lower runtime compat wiring | Keep in active investigation: bundling lower legacy timer/runtime, scheduler, task-entry, and semaphore-lock behavior under `MEDITAMER_WIFI_BACKEND_LEGACY_PORT_DIAG=1` moved the failure boundary forward from hard panics (`delete_task`, `schedule_wakeup`, non-reentrant semaphore give) to a post-`init_tasks_precreate_timer_task` stall. Not a fix yet, but confirms the true backend port is exercising deeper legacy runtime behavior. | active investigation / not yet accepted | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [semfix log](../../logs/boot_scan_backend_legacy_port_mode_20260310_semfix/monitor_clean.log) |
| isolated current `esp-radio` with legacy bootstrap shim inserted at actual `esp_radio::init()` boundary | Reject as sufficient fix. The first real runtime-port experiment inserted the legacy bootstrap shim directly into current `esp_radio::init()` after `setup_radio_isr()` and before `wifi_set_log_verbose()` / `init_radio_clocks()`, under `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_BOOTSTRAP_SHIM_DIAG=1`. The isolated current standalone comparator still reached clean startup (`begin`, `esp_radio_init`, `wifi_new`, `start`) and still ended at `scan=ok count=0`, with the same late queue-family behavior. This closes the narrow “precreate+yield shim at current init time is enough to recover legacy bootstrap behavior” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-24](./upload-throughput-history/part-24.md), [legacy-bootstrap-shim PTY summary](../../logs/esp_radio_nostd_wifi_control_legacybootstrapshim_20260309_berlin/pty_summary.txt) |
| isolated current `esp-radio` with strengthened legacy bootstrap shim waiting for timer-task entry | Reject as sufficient fix. Strengthening the same runtime-port shim so it waits for `timer` task entry rather than just creating the task and yielding once still preserves clean startup in the isolated current standalone comparator and still ends at `scan=ok count=0`, with the same late queue-family shape. This closes the stronger “legacy bootstrap can be recovered by ensuring the timer task actually runs early” branch and pushes the blocker below bootstrap-precreate/yield semantics. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-24](./upload-throughput-history/part-24.md), [stronger bootstrap-shim PTY summary](../../logs/esp_radio_nostd_wifi_control_legacybootstrapshim2_20260309_berlin/pty_summary.txt) |
| isolated current `esp-radio` with legacy-style HAL `_esp_timer_get_time` source (`MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_ESP_TIMER_GET_TIME_DIAG=1`) | Reject as sufficient fix. Forcing current `__esp_radio_esp_timer_get_time()` to use the legacy-style HAL microsecond source (`esp_hal::time::Instant::now().duration_since_epoch().as_micros()`) instead of scheduler-backed `preempt::now()` preserves clean isolated current-stack startup but still ends at `scan=ok count=0`, with the same late queue-family behavior as prior failing runs. This closes the remaining novel timer-source wrapper branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-24](./upload-throughput-history/part-24.md), [legacy-time standalone log](../../logs/esp_radio_nostd_wifi_control_legacytime_20260309_berlin/monitor.log) |
| unified firmware legacy-port path (`MEDITAMER_WIFI_BACKEND_LEGACY_PORT_DIAG=1`) | Reject as sufficient fix. The new unified `backend_legacy_port` path does execute in the main firmware (`legacy_port_runtime ...`, `legacy_port_bootstrap ...`, `legacy_port runtime_init result=ok`) and also enables legacy-style synchronous backend `start/scan/stop`, but a bounded boot-scan validation still kept the pre-scan promisc window fully dark and still returned zero APs from direct IDF `NULL`, direct IDF explicit broad, and wrapped backend scans. This closes the branch “current firmware legacy-port shim is enough” and pushes the next work to a deeper source-level legacy runtime/backend port. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-24](./upload-throughput-history/part-24.md), [legacy-port firmware validation log](../../logs/boot_scan_backend_legacy_port_20260309_172022/monitor.log) |
| unified firmware legacy-port path with explicit legacy-preempt compatibility surface (`MEDITAMER_WIFI_BACKEND_LEGACY_PORT_DIAG=1`) | Reject as sufficient fix. The deeper vendored runtime compatibility surface now reports `scheduler_initialized=true`, `current_core_initialized=true`, `timer_task_precreated=true`, `timer_task_started=true`, and `yielded_once=true` through the active `backend_legacy_port` firmware path, but a bounded boot-scan validation still kept the pre-scan promisc window fully dark and still returned zero APs from direct IDF `NULL`, direct IDF explicit broad, and wrapped backend scans. This closes the branch “legacy-preempt compatibility is enough” and leaves the deeper source-level legacy runtime/backend port as the remaining path. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy-preempt legacy-port firmware validation log](../../logs/boot_scan_backend_legacy_port_preemptcompat_20260309_173535/monitor.log) |
| isolated current `esp-radio` with legacy-style Xtensa task entry (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_TASK_ENTRY_DIAG=1`) | Reject as sufficient fix. Forcing current `esp-rtos` to start `esp-radio` tasks with legacy-style direct Xtensa task entry (`PC=task_fn`, `A6=param`) instead of the current `task_wrapper` trampoline preserves clean isolated current-stack startup but still ends at `scan=ok count=0`. This closes another concrete task/preempt runtime branch: task entry register setup alone is not the discriminator. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy-task-entry standalone log](../../logs/esp_radio_nostd_wifi_control_legacytaskentry_validate_20260309_berlin/monitor.log) |
| isolated current `esp-radio` with legacy Wi‑Fi task handoff after create (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_WIFI_TASK_BOOTSTRAP_DIAG=1`) | Reject as sufficient fix. Forcing current `esp-rtos` to yield repeatedly until the newly created `wifi` task has been selected at least once still preserves clean isolated current-stack startup and still ends at `scan=ok count=0`. This closes the simple “legacy first handoff” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy-WiFi-handoff standalone log](../../logs/esp_radio_nostd_wifi_control_legacywifihandoff_20260309_berlin/monitor.log) |
| isolated current `esp-radio` with legacy zero-priority Wi‑Fi task model plus handoff (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_WIFI_TASK_PRIORITY_MODEL_DIAG=1`) | Reject as sufficient fix. Combining the explicit `wifi` task handoff with a legacy-like zero-priority `wifi` task still preserves clean isolated current-stack startup and still ends at `scan=ok count=0`. This closes the next scheduler-model approximation and leaves the deeper run-queue/circular-scheduler semantics as the remaining scheduler-level gap. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy-priority-handoff standalone log](../../logs/esp_radio_nostd_wifi_control_legacypriorityhandoff_20260309_berlin/monitor.log) |
| isolated current `esp-radio` with corrected three-task legacy task model (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_ESP_RADIO_TASK_MODEL_DIAG=1`) | Reject as sufficient fix. Extending the vendored legacy task-model ring to include the internal `timer` task and fixing the `wifi` index selection bug does change blob-facing task identity as intended (`legacy_task_model_entry_count=3`, `legacy_task_model_current_index=2`, recent `task_get_current_task` samples resolve to `wifi`), but pre-scan promisc still stays zero and the isolated current standalone still ends at `scan=ok count=0`. This closes the legacy task-model bookkeeping branch and leaves the deeper circular/run-queue scheduler behavior as the remaining scheduler-level target. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy-task-model timer-fix summary](../../logs/esp_radio_nostd_wifi_control_legacytaskmodel_timerfix_20260309_berlin/summary.txt) |
| isolated current `esp-radio` with legacy task insertion order in the diagnostic ring (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_ESP_RADIO_TASK_MODEL_DIAG=1`) | Reject as sufficient fix. Changing the vendored legacy task-model ring so newly created tasks are inserted immediately after the current task reproduces the legacy circular ordering (`main -> wifi -> timer`, with `legacy_task_model_current_index=1` after `wifi_new`), but pre-scan promisc still stays zero and the isolated current standalone still ends at `scan=ok count=0`. This closes the task-ring ordering branch and leaves the deeper run-queue scheduler behavior as the remaining scheduler-level target. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy-task-model insertion summary](../../logs/esp_radio_nostd_wifi_control_legacytaskmodel_insert_20260309_berlin/summary.txt) |
| isolated current `esp-radio` with real legacy run-queue selection plus forced timeslice (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_ESP_RADIO_TASK_MODEL_DIAG=1`) | Reject as sufficient fix. Extending the vendored runtime so `SchedulerState::run_scheduler()` prefers the legacy task ring for next-task selection and always arms the next timeslice when more than one legacy task is ready does materially change scheduler-visible behavior (`legacy_task_model_entry_count=3`, `legacy_task_model_current_index=0`, `wifi_task_selected_count=9` after `wifi_new`), but pre-scan promisc still stays zero and the isolated current standalone still ends at `scan=ok count=0`. This closes the deeper run-queue/timeslice branch too. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-25](./upload-throughput-history/part-25.md), [legacy run-queue summary](../../logs/esp_radio_nostd_wifi_control_legacyrunqueue_20260309_berlin/summary.txt) |
| isolated current `esp-radio` with legacy ring advancement moved from `yield_task()` to actual task selection (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_ESP_RADIO_TASK_MODEL_DIAG=1`) | Reject as sufficient fix. Correcting the vendored legacy scheduler shim so the diagnostic ring advances only when the scheduler actually selects a task, not on every `yield_task()` request, still leaves the isolated current standalone with `legacy_task_model_entry_count=3`, `legacy_task_model_current_index=0`, zero pre-scan promisc packets, and `scan=ok count=0`. This closes the remaining easy yield-vs-select bookkeeping branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-26](./upload-throughput-history/part-26.md), [legacy run-queue selection summary](../../logs/esp_radio_nostd_wifi_control_legacyrunqueue_select_20260309_berlin/summary.txt) |
| isolated current `esp-radio` with legacy-task-model readiness bypassing `RunQueue` (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_ESP_RADIO_TASK_MODEL_DIAG=1`) | Reject in current form. Bypassing `RunQueue` readiness bookkeeping for esp-radio task creation/resume and scheduler requeue regresses earlier than prior scheduler branches: bootloader output remains normal, but the isolated current standalone never reaches any application log line after boot. This closes the first direct run-queue-bypass attempt as too aggressive in its current implementation. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-26](./upload-throughput-history/part-26.md), [legacy run-queue bypass monitor](../../logs/esp_radio_nostd_wifi_control_legacyrunqueue_bypass_20260309_berlin/monitor.log) |
| isolated current `esp-radio` routed through the first in-tree legacy built-in scheduler state (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_BUILTIN_SCHEDULER_DIAG=1`) | Reject as sufficient fix. Routing esp-radio scheduler-facing task creation/current-task/thread-semaphore/task-deletion calls through the new in-tree legacy built-in scheduler state still preserves clean isolated startup (`begin=true`, `esp_radio_init=ok`, `wifi_new=ok`, `start=ok`) but still ends at `scan=ok count=0`, with the same scan-tail queue families (`0x6`, `0x10`, `0x7/0x8/0x0`, consumer-side `0x17`). This closes the first source-level legacy built-in scheduler substitution branch as insufficient. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-26](./upload-throughput-history/part-26.md), [legacy built-in scheduler summary](../../logs/esp_radio_nostd_wifi_control_legacybuiltin_20260309_berlin/summary.txt) |
| isolated current `esp-radio` with the in-tree legacy built-in scheduler owning actual context switching (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_BUILTIN_SCHEDULER_DIAG=1`) | Reject as sufficient fix. Extending the in-tree legacy built-in scheduler so it also owns real circular-list task switching still preserves clean isolated startup (`begin=true`, `esp_radio_init=ok`, `wifi_new=ok`, `start=ok`, `stop=ok`) but still ends at `scan=ok count=0`, with the same scan-tail queue families (`0x6`, `0x10`, `0x7/0x8/0x0`, consumer-side `0x17`). This closes the first source-level legacy built-in scheduler switch path too. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-26](./upload-throughput-history/part-26.md), [legacy built-in scheduler switch summary](../../logs/esp_radio_nostd_wifi_control_legacybuiltin_switch_20260309_berlin/summary.txt) |
| isolated current `esp-radio` with the old `preempt_builtin` Xtensa timer/multitasking slice ported in (`MEDITAMER_WIFI_ESP_RTOS_USE_LEGACY_PREEMPT_BUILTIN_TIMER_DIAG=1`) | Reject as sufficient fix. Porting the first real source-level slice of the old `preempt_builtin` layer into vendored runtime support, specifically legacy Xtensa interrupt-mask setup plus legacy-style periodic timeslice ticking, still preserves clean isolated startup (`begin=true`, `esp_radio_init=ok`, `wifi_new=ok`, `start=ok`, `stop=ok`) but still ends at `scan=ok count=0`, with the same scan-tail queue families (`0x6`, `0x10`, `0x7/0x8/0x0`, consumer-side `0x17`). This closes the first real `preempt_builtin` timer/multitasking port branch as insufficient. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-26](./upload-throughput-history/part-26.md), [legacy preempt-builtin timer summary](../../logs/esp_radio_nostd_wifi_control_legacypreempttimer_20260309_berlin/summary.txt) |
| current `esp-radio` with true legacy `timer_compat` source port (`MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_TIMER_COMPAT_DIAG=1`) | Treat as the first material recovery branch, but not yet a full fix. Porting the legacy linked-list `compat/timer_compat` source-level implementation restores the isolated current standalone `esp-radio` path (`scan=ok count=5`), proving the current stack can scan once its timer-compat layer matches the working legacy generation more closely. The same branch does not yet restore the full firmware boot-scan path, which still shows zero pre-scan promisc packets, zero direct-IDF explicit APs, and zero wrapped scan results. The root-cause boundary therefore moves upward from “core runtime can never scan” to “full firmware path or extra diagnostic sequence still suppresses discovery.” | diagnostic outcome / primary surviving branch | 2026-03-09 | [History part-26](./upload-throughput-history/part-26.md), [isolated legacy-timer-compat log](../../logs/esp_radio_nostd_wifi_control_legacytimercompat_20260309_berlin/monitor.log), [main firmware legacy-timer-compat log](../../logs/boot_scan_legacytimercompat_main_20260309_berlin/monitor.log) |
| `backend_legacy_port` runtime mode now implies legacy timer/runtime compat | Treat as structural port progress, not yet validated as a behavioral fix. `MEDITAMER_WIFI_BACKEND_LEGACY_PORT_DIAG=1` now also enables the lower legacy timer/runtime path directly in vendored `esp-radio`/`esp-rtos`, instead of depending on separate timer-compat bootstrap knobs. This makes `backend_legacy_port` closer to a true backend/runtime mode and reduces mode skew between firmware-layer backend selection and low-level runtime behavior. | structural migration step | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md) |
| current `esp-radio` with exact old `timer_task()` yield-loop semantics on top of legacy timer compat (`MEDITAMER_WIFI_ESP_RTOS_USE_EXACT_LEGACY_TIMER_COMPAT_LOOP_DIAG=1`) | Reject as sufficient fix. After the legacy timer-compat source port was corrected so timers were genuinely registered, armed, and executed, forcing the old `esp-wifi 0.15.1` yield-only `timer_task()` loop shape in the isolated current comparator still did not restore discovery. Startup reached `begin`, `esp_radio_init`, `wifi_new`, and `start`, but the bounded run no longer even reached pre-scan promisc output or the scan result lines. This closes the exact timer-task loop cloning branch and shows the remaining gap is deeper than legacy timer-loop shape alone. | diagnostic outcome / rejected causal fix | 2026-03-10 | [History part-26](./upload-throughput-history/part-26.md), [exact legacy timer-loop standalone log](../../logs/esp_radio_nostd_wifi_control_exactlegacytimerloop_20260310_berlin/monitor.log) |
| current `esp-radio` with legacy timer-compat plus legacy `init_tasks()` timing (`MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_TIMER_COMPAT_INIT_TASKS_DIAG=1`) | Reject as sufficient fix. Moving timer-task creation to immediately after `esp_radio::init()`, closer to the old `esp-wifi 0.15.1` `init_tasks()` timing, still leaves the isolated current standalone stack fully RX-dark. The branch reaches `begin`, `esp_radio_init`, `precreate_timer_task=after_esp_radio_init`, `wifi_new`, and `start`, and the legacy timer-compat layer remains live (`setfn_count=18 arm_count=26 exec_count=13`), but pre-scan promisc stays zero and the wrapped scan still ends at `scan=ok count=0`. This closes timer-task placement as the primary remaining cause. | diagnostic outcome / rejected causal fix | 2026-03-10 | [History part-26](./upload-throughput-history/part-26.md), [legacy init-tasks standalone log](../../logs/esp_radio_nostd_wifi_control_legacyinittasks_20260310_berlin/monitor.log) |
| main firmware boot-scan with `sd_task` suppressed during the diagnostic window | Reject `sd_task` concurrency as the primary remaining firmware-path cause. Even when runtime bootstrap explicitly skips spawning `sd_task` while `MEDITAMER_WIFI_BOOT_SCAN_ONLY_DIAG=1` is active, the main firmware still times out the wrapped scan, still ends the final direct-IDF `NULL` compare at `ap_num=0`, and still reaches raw `ScanDone` with an empty list. This closes the obvious runtime-side SD-probe interference branch in the reduced firmware path. | diagnostic outcome / rejected causal fix | 2026-03-10 | [History part-26](./upload-throughput-history/part-26.md), [sd-task-suppressed firmware log](../../logs/boot_scan_legacytimercompat_min_nosd_20260309_berlin/monitor.log) |
| backend legacy sync start/scan/stop compatibility path (`MEDITAMER_WIFI_BACKEND_LEGACY_SYNC_START_SCAN_DIAG`) | Keep default `off`; reject as sufficient fix. This reproduces the working legacy no-std `esp-wifi 0.15.1` synchronous `start/scan/stop` controller semantics inside the main firmware backend, but a live main-firmware boot-scan run with the knob enabled still kept the pre-scan promisc window fully dark on channels `8/1/6/11` and still returned zero APs from wrapped, direct IDF `NULL`, and direct IDF explicit scans. Legacy synchronous start/scan/stop semantics alone do not restore RX visibility inside the main firmware path. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md), [legacy-sync firmware log](../../logs/boot_scan_legacysync_20260309_150800/monitor_espflash.log) |
| isolated current `esp-radio` with `MEDITAMER_WIFI_ESP_RADIO_SKIP_MAC_TIME_UPDATE_CB_DIAG=1` | Reject as sufficient fix. Skipping the current esp32-only `set_mac_time_update_cb()` hook before `esp_wifi_init_internal()` keeps the isolated current standalone startup clean but leaves the pre-scan promisc window fully dark on channels `8/1/6/11` and still ends at `scan=ok count=0`. This closes the “pre-init MAC time update callback is the primary cause” branch. | diagnostic outcome / rejected causal fix | 2026-03-09 | [History part-23](./upload-throughput-history/part-23.md), [skip-mac-time standalone log](../../logs/esp_radio_nostd_wifi_control_skipmactime_20260309_berlin/monitor.log) |
| `backend_legacy_port` with real per-task counting semaphores in legacy built-in scheduler state | Keep as required structural fix, but not a full discovery fix. Replacing the legacy built-in scheduler’s raw `u32` thread-semaphore slots with real counting `Semaphore` objects unblocks the `backend_legacy_port` firmware path past the former `esp_wifi_init_internal(...)` deadlock: the main-thread semaphore give succeeds, `wifi_new` completes, `esp_supplicant_init` completes, both internal RX callbacks register, and boot-scan reaches `start=ok`. But pre-scan promisc still stays zero on channels `8/1/6/11`, so the primary blocker moves later to the fully initialized-but-RX-dark scan path rather than disappearing. | structural fix / boundary moved later | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [thread-semaphore fix firmware log](../../logs/boot_scan_backend_legacy_port_mode_20260310_110951_threadsemfix/monitor_clean.log) |
| `backend_legacy_port` forcing wrapped scans through direct explicit IDF scan (`MEDITAMER_WIFI_BACKEND_LEGACY_PORT_FORCE_DIRECT_EXPLICIT_SCAN_DIAG=1`) | Reject as sufficient fix. On the corrected `backend_legacy_port` runtime, forcing wrapped scans through a direct blocking explicit `esp_wifi_scan_start(&wifi_scan_config_t, true)` path does not restore discovery: pre-scan promisc stays zero, direct IDF `NULL` and direct IDF explicit scans still return zero APs, wrapped scan also returns zero APs, and raw `ScanDone` lists remain empty. Legacy timer compat is active (`setfn_count=6 arm_count=65 exec_count=39`), but the deeper timer-runtime counters stay flat (`entry_count=0 mark_ready_count=0 pop_count=0 selected_count=0`), keeping the remaining blocker in the legacy timer/runtime execution path rather than scan-wrapper shape. | diagnostic outcome / rejected causal fix | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [force-direct-explicit backend log](../../logs/boot_scan_backend_legacy_port_mode_20260310_160737_force_direct_explicit_fixed/monitor_retry.log) |
| symmetric raw promiscuous callback controls on working legacy no-std vs failing current `esp-radio` | Treat as the final wrapper-level boundary closure. The isolated current `esp-radio` standalone now uses the same direct raw promiscuous callback path (`esp_wifi_get_promiscuous`, `esp_wifi_set_promiscuous_rx_cb`, `esp_wifi_set_promiscuous_filter`, `esp_wifi_set_promiscuous(true/false)`) as firmware diagnostics and still sees zero packets on `8/1/6/11` before `scan=ok count=0`. The working legacy no-std standalone uses that same raw path and sees packets on all sampled channels (`total=10 mgmt=8 data=2`) before `scan=ok count=5`. This closes the remaining “Sniffer/helper wrapper” ambiguity and pins the surviving boundary below wrapper/direct promiscuous setup and below wrapper/direct scan invocation, in the current `esp-radio` / runtime RX-admission path itself. | diagnostic outcome / primary boundary tightened | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [current raw-promisc standalone summary](../../logs/esp_radio_nostd_wifi_control_rawpromisc_correctelf_flashonly_20260310_163856/summary.txt), [legacy raw-promisc standalone summary](../../logs/esp_wifi_legacy_nostd_control_rawpromisc_manual_20260310_164438/summary.txt) |
| raw-promisc ISR-window comparison on working legacy no-std vs failing current `esp-radio` | Treat as the next boundary tightening below wrapper level. Both isolated stacks now log `wifi_mac_isr` counts around the same raw-promiscuous window. The failing current stack still sees zero packets, but `WIFI_MAC` interrupts advance from `0` to `7` during the promisc window and to `36` by scan completion. The working legacy stack sees non-zero packets in the same raw-promisc window while `WIFI_MAC` interrupts advance from `0` to `8` and then to `44`. This closes “missing interrupts” as the explanation for the current dark promisc window and moves the surviving boundary down to packet admission/delivery between `WIFI_MAC` interrupt activity and promiscuous/scan callback visibility. | diagnostic outcome / primary boundary tightened | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [current raw-promisc ISR summary](../../logs/esp_radio_nostd_wifi_control_rawpromisc_isr_20260310_165216/summary.txt), [legacy raw-promisc ISR summary](../../logs/esp_wifi_legacy_nostd_control_rawpromisc_isr_20260310_165306/summary.txt) |
| current isolated `esp-radio` shows live `WIFI_MAC` interrupts but zero internal RX callback delivery | Treat as the next exact boundary. In the isolated current standalone, `wifi_mac_isr` advances during promisc and scan (`after_promisc=11`, `after_scan=39`) while `wifi_rx_cb_diag` remains `sta=0 ap=0` after `wifi_new`, `wifi_start`, and `scan`, and raw promisc plus scan results both stay zero. This proves the surviving failure is earlier than `recv_cb_sta/ap` queueing and earlier than scan/BSS admission, narrowing the remaining target to the first blob-facing packet delivery step before the registered internal RX callbacks fire. | diagnostic outcome / primary boundary tightened | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [current rx-callback boundary log](../../logs/esp_radio_nostd_wifi_control_gmiscnvs_20260310_berlin/monitor.log) |
| current isolated `esp-radio` with legacy `queue_send_from_isr` semantics (`MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_QUEUE_SEND_FROM_ISR_DIAG=1`) | Treat as the first positive packet-delivery branch on the current generation. Forcing current `queue_send_from_isr` to behave like the working legacy path restores non-zero raw promisc packet delivery on the isolated current standalone (`total=5 mgmt=5` across sampled channels), proving the current RX-dark boundary can be moved by changing blob-facing ISR queue-send semantics. The same run then panicked before scan completion due to diagnostic code calling `preempt::current_task()` in a context with no current task, so scan recovery is not yet proven. This branch is a strong causal lead, not a promoted fix yet. | diagnostic outcome / strong causal lead | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [legacy isr-send standalone log](../../logs/esp_radio_nostd_wifi_control_legacyisrsend_apponly_20260310_183925/monitor_retry2.log) |
| `backend_legacy_port` main firmware with legacy `queue_send_from_isr` semantics plus slim app image | Reject as sufficient carry-over of the standalone lead. On the smaller main firmware image (`wifi-debug-slim-app`) with `MEDITAMER_WIFI_BACKEND_LEGACY_PORT_DIAG=1` and `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_QUEUE_SEND_FROM_ISR_DIAG=1`, boot scan starts cleanly and `WIFI_MAC` interrupts advance, but pre-scan promisc still stays zero on `8/1/6/11`, internal RX callbacks remain `sta=0 ap=0`, and wrapped/direct scans still return zero APs. This means the positive standalone ISR-send result does not automatically carry over to the current legacy-port firmware path; the next target is the runtime difference between those two current-generation paths. | diagnostic outcome / not sufficient in firmware path | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [legacy isr-send backend firmware log](../../logs/boot_scan_backend_legacy_port_legacyisrsend_slim_20260310_berlin/monitor_espflash.log) |
| current worktree standalone `esp-radio` control with only legacy `queue_send_from_isr` enabled | Keep as a regression note, not a promoted lead. Re-running the isolated current standalone on the current worktree with only `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_QUEUE_SEND_FROM_ISR_DIAG=1` now yields `scan=ok count=0`, `wifi_mac_isr_diag after_scan=39`, and `wifi_promisc_cb_diag after_scan=0`. This means the earlier positive standalone ISR-send result is no longer reproduced in the present worktree and the newer runtime-slice bisect must be interpreted as regression-sensitive until that standalone control is restored. | diagnostic outcome / regression note | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [current-worktree ISR-send-only control log](../../logs/esp_radio_nostd_wifi_control_isrsend_only_control_20260310_191306/monitor.log) |
| current worktree standalone `esp-radio` with exact legacy ISR-delivery wrapper bundle | Reject as sufficient fix. Enabling the combined legacy blob-facing ISR-delivery wrapper bundle on the isolated current standalone (`legacy queue_send_from_isr`, `legacy semaphore_from_isr`, `legacy task_yield_from_isr`, and legacy `coex_status_get`) still yields `begin=true`, `esp_radio_init=ok`, `wifi_new=ok`, `start=ok`, but raw promisc remains fully dark (`after_promisc count=0`) and wrapped scan still ends at `scan=ok count=0` while `WIFI_MAC` IRQs advance. This closes the remaining easy ISR-wrapper class and leaves the surviving boundary below those hooks, between live IRQ activity and first packet delivery. | diagnostic outcome / rejected causal fix | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [current-worktree IRQ bundle control log](../../logs/esp_radio_nostd_wifi_control_irqbundle_20260310_193543/monitor_retry.log) |
| current standalone `esp-radio` with explicit ISR-yield after legacy ISR queue-send | Keep as the current tightest packet-delivery boundary. On the failing current standalone path, adding the missing wake step directly to the legacy ISR-send branch raises both `queue_send_from_isr` and `task_yield_from_isr` during the raw-promisc/scan window (`after_promisc queue_send_isr=3 task_yield_from_isr=6`, `after_scan queue_send_isr=17 task_yield_from_isr=6`), while `WIFI_MAC` interrupts continue to advance and raw promiscuous callback delivery still remains zero. This closes ISR wake semantics as the primary cause and moves the surviving boundary below ISR send/yield behavior and above first delivered packet/callback admission. | diagnostic outcome / primary boundary tightened | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [current ISR-yield summary](../../logs/esp_radio_nostd_wifi_control_isryield_20260310_200506/summary_postflash.txt) |
| `backend_legacy_port` explicit vendored `init_tasks()` bootstrap | Keep as active migration progress, not a concluded fix. The legacy backend path now uses explicit vendored `init_legacy_wifi_tasks()` plus a dedicated `legacy_timer_task` entrypoint instead of the generic `precreate_esp_radio_timer_task()` shortcut. This is a structural source-level port step aligned with the vendoring plan; it keeps the remaining work focused on true backend/runtime migration rather than more generic `esp-radio` hook churn. | active migration step | 2026-03-10 | [History part-27](./upload-throughput-history/part-27.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |
| dedicated legacy Wi-Fi os-adapter module for `backend_legacy_port` | Keep and build on this split. The legacy backend path now routes Wi-Fi callback surfaces through `vendor/esp-radio-0.17.0/src/wifi/os_adapter/legacy_backend.rs` instead of embedding legacy semaphore/mutex/task/thread behavior inline inside the mixed modern `wifi/os_adapter/mod.rs` body. This is a structural vendoring step, not a new hook experiment, and it reduces dependence on the modern adapter body before the next runtime validation. | active migration step | 2026-03-11 | [History part-27](./upload-throughput-history/part-27.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |
| canonical validation of dedicated legacy Wi-Fi os-adapter path | Treat as a structural step that did not move the runtime boundary. With the dedicated legacy Wi-Fi os-adapter backend active, `backend_legacy_port` still reaches `legacy_port_wifi_init stage=esp_wifi_init_internal.before` and then stops inside `esp_wifi_init_internal(...)`. This keeps the active wholesale-port boundary inside the init substrate expected by the newer blob generation, not in the mixed modern os-adapter body anymore. | migration validation / boundary unchanged | 2026-03-11 | [History part-27](./upload-throughput-history/part-27.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |
| dedicated legacy common-adapter backend module for `backend_legacy_port` | Keep and build on this split. The legacy backend path now routes blob-facing semaphore/queue callbacks through `vendor/esp-radio-0.17.0/src/compat/common_adapter_legacy_backend.rs` instead of embedding legacy queue/semaphore behavior inline inside the mixed modern `common_adapter.rs` body. This is a structural vendoring step aligned with the wholesale migration plan and removes another mixed callback layer from `backend_legacy_port` before the next canonical validation. | active migration step | 2026-03-11 | [History part-28](./upload-throughput-history/part-28.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |

## Update Rules

1. Add one row per durable decision (promoted, rejected, non-default, fixed).
2. Link each row to at least one RFC step or history section.
3. When a decision is superseded, keep the old row and mark the new row as latest.
4. Keep this ledger concise; details stay in RFC/history shards.
- Porting legacy `g_misc_nvs` assignment into current `esp-radio` `wifi_init()` did not restore raw-promisc packets or scan results in the isolated current control.
- Completing legacy alloc parity for `zalloc_internal`, `wifi_calloc`, and `wifi_zalloc` under the current legacy-alloc knob also did not restore raw-promisc packets or scan results.
| 2026-03-11 | Dedicated legacy preempt adapter for `backend_legacy_port` | `backend_legacy_port` no longer depends on the mixed `preempt_backend` selector for task/thread-semaphore operations; canonical validation still stalls immediately after `wifi_thread_semphr_get.after` inside `esp_wifi_init_internal(...)`, so the next slice must replace/import the legacy `preempt_builtin` runtime more literally. | `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/docs/development/upload-throughput-history/part-28.md` |
| dedicated legacy common-adapter validation for `backend_legacy_port` | Treat as a structural port step that did not move the runtime boundary. With the dedicated legacy common-adapter backend active, canonical slim-app validation still reaches `legacy_port_wifi_init stage=esp_wifi_init_internal.before` and then stops inside `esp_wifi_init_internal(...)`. This confirms the migration is no longer blocked in mixed semaphore/queue adapter glue; the next wholesale slice must move deeper into the literal legacy runtime/init substrate. | migration validation / boundary unchanged | 2026-03-11 | [History part-28](./upload-throughput-history/part-28.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |
| 2026-03-12 | `backend_legacy_port` direct `common_legacy` routing | `vendor/esp-radio-0.17.0/src/compat/common_adapter_legacy_backend.rs` routed directly to imported `compat/common_legacy.rs`; canonical slim-app `hostctl flash-capture` still stopped at `legacy_port_wifi_init stage=esp_wifi_init_internal.before` | Closed; next wholesale slice moves inward to literal internal Wi-Fi backend/init substrate |
| 2026-03-12 | `backend_legacy_port` literal legacy `scan_n()` broad-scan path | Replacing the broad unfiltered active scan path with the working legacy blocking `scan_n()` shape did not restore packet delivery. Canonical full-flash validation still reaches `start=ok`, but pre-scan promisc remains zero, wrapped scan still fails early with `InternalError(Timeout)`, and direct explicit scan still returns `status=1 count=0` with empty raw `ScanDone` state. This closes scan-wrapper shape as the primary gap in the wholesale port and leaves the boundary below scan invocation shape and above packet delivery / scan admission. | [History part-28](./upload-throughput-history/part-28.md), `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/logs/flash_capture_20260312_130831/capture.log` |
| 2026-03-12 | `backend_legacy_port` dedicated legacy RX delivery backend | Added `vendor/esp-radio-0.17.0/src/wifi/legacy_rx_backend.rs` and routed `legacy_delivery` through it so queue storage and sniffer callback storage are now owned by a dedicated legacy packet-delivery module instead of the mixed modern path. This is a wholesale migration step; validate it only through canonical `backend_legacy_port` flash/capture. | [History part-28](./upload-throughput-history/part-28.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |
| 2026-03-12 | `backend_legacy_port` direct legacy os-adapter core path | Re-routing core task/mutex/thread-semaphore callbacks through `vendor/esp-radio-0.17.0/src/wifi/os_adapter/legacy_backend.rs` did not restore packet delivery. Canonical full-flash validation still reaches `start=ok`, but pre-scan promisc remains zero, direct/wrapped scans still return zero or timeout, `wifi_mac_isr_count` rises, and the scan-time runtime counters remain flat (`queue_send=0`, `queue_send_isr=0`, `queue_recv=0`, `sem_take=0`, `sem_give=0`, `thread_sem_get=0`). This closes the core os-adapter reroute as a sufficient step and pins the next wholesale slice on persistent legacy preempt/scheduler runtime participation after init. | [History part-28](./upload-throughput-history/part-28.md), `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/logs/flash_capture_20260312_162924/capture.log` |
| 2026-03-13 | `backend_legacy_port` dedicated internal legacy packet backend | Switching `legacy_port_wifi_init()` RX callback registration onto `vendor/esp-radio-0.17.0/src/wifi/internal_legacy_packet_backend.rs` did not restore packet delivery. Canonical full-flash validation still reaches `runtime_init result=ok` and `start=ok`, but pre-scan promisc remains zero, `wifi_mac_isr_count` rises while `wifi_rx_cb_count sta=0 ap=0`, raw `ScanDone` lists remain empty, and wrapped/direct scans still fail with timeout or zero APs. This closes the packet-callback registration surface as the primary remaining gap and leaves the boundary in the deeper internal Wi‑Fi delivery/admission path itself. | [History part-28](./upload-throughput-history/part-28.md), `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/logs/hostctl_flashcapture_backend_legacy_port_20260313_100539_internal_legacy_packet/capture.log` |
| 2026-03-13 | `backend_legacy_port` literal legacy Wi-Fi os-adapter backend | Replaced `wifi/legacy_osi_backend.rs` delegation away from the compat shim `compat/common_adapter_legacy_osi_backend` and onto a dedicated literal backend module `vendor/esp-radio-0.17.0/src/wifi/legacy_literal_backend.rs`, sourced from the ported `internal_legacy_common_backend` surface. This is a wholesale migration step that consolidates semaphore/queue/mutex/task/time/event helpers into one internal legacy backend before the next canonical validation. | [History part-28](./upload-throughput-history/part-28.md), [Wi-Fi Legacy Vendoring Plan](./wifi-legacy-vendoring-plan.md) |
| 2026-03-16 | `backend_legacy_port` literal legacy admission module | Collapsing `backend_legacy_port` start/stop/scan into one literal legacy admission module (`vendor/esp-radio-0.17.0/src/wifi/internal_legacy_admission_literal.rs`) did not move the discovery boundary. Canonical full-flash boot-scan validation still reaches `runtime_init result=ok` and `boot_scan_only_diag start=ok`, but pre-scan promisc remains zero, direct null scan still fails with `scan_rc=12300`, direct explicit scan still returns `ap_num=0`, wrapped scan still fails with `InternalError(Timeout)`, `wifi_mac_isr_count` rises, `wifi_rx_cb_count sta=0 ap=0`, raw `ScanDone` remains empty, and scan-time queue/semaphore/thread-semaphore counters stay flat. This closes the Rust-side internal admission wrapper split as the primary remaining gap and leaves the blocker in the deeper internal Wi‑Fi/blob delivery path. | [History part-28](./upload-throughput-history/part-28.md), `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/logs/hostctl_flashcapture_backend_legacy_port_20260316_075226_literal_admission_bootscan/capture.log` |
| 2026-03-16 | `backend_legacy_port` literal legacy event path | Routing legacy `event_post` through a dedicated internal legacy event module (`vendor/esp-radio-0.17.0/src/wifi/internal_legacy_event_literal.rs`) did not move the discovery boundary. Canonical full-flash boot-scan validation still reaches `runtime_init result=ok` and `boot_scan_only_diag start=ok`, but pre-scan promisc remains zero, direct null scan still fails with `scan_rc=12300`, direct explicit scan still returns `ap_num=0`, wrapped scan still fails with `InternalError(Timeout)`, `wifi_mac_isr_count` rises, `wifi_rx_cb_count sta=0 ap=0`, raw `ScanDone` remains empty, `scan_done_eventpost` remains zero, and scan-time queue/semaphore/thread-semaphore counters stay flat. This closes the mixed modern Wi-Fi event handler surface as the primary remaining gap and leaves the blocker in the deeper internal Wi‑Fi/blob delivery/admission path. | [History part-29](./upload-throughput-history/part-29.md), `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/logs/hostctl_flashcapture_backend_legacy_port_20260316_081114_legacy_event_literal/capture.log` |
| 2026-03-16 | `backend_legacy_port` literal internal facade | Consolidating the active legacy path behind `vendor/esp-radio-0.17.0/src/wifi/internal_legacy_facade.rs` did not move the discovery boundary. Canonical full-flash boot-scan validation still reaches `runtime_init result=ok`, `legacy_port_wifi_init stage=done`, and `boot_scan_only_diag start=ok`, but pre-scan promisc remains zero, direct null scan still fails with `scan_rc=12300`, direct explicit scan still returns `ap_num=0`, wrapped scan still fails with `InternalError(Timeout)`, `wifi_mac_isr_count` rises, `wifi_rx_cb_count sta=0 ap=0`, raw `ScanDone` remains empty, `scan_done_eventpost` remains zero, and scan-time queue/semaphore/thread-semaphore counters stay flat. This closes stitched Rust-side legacy entrypoints as the primary remaining gap and leaves the blocker deeper in the internal Wi‑Fi/blob delivery or admission behavior. | [History part-29](./upload-throughput-history/part-29.md), `/Users/dimitri/Documents/Code/personal/Inkplate/meditamer/logs/hostctl_flashcapture_backend_legacy_port_20260316_internal_facade_chunk/capture.log` |
