# Upload Throughput History Part 27 (continued 2)

[Back to Part 27](./part-27.md).

## 2026-03-10 Current worktree no longer reproduces the earlier standalone `legacy queue_send_from_isr` positive control

- Re-ran the isolated current standalone on the current worktree with only:
  - `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_QUEUE_SEND_FROM_ISR_DIAG=1`
- Validation log:
  - `logs/esp_radio_nostd_wifi_control_isrsend_only_control_20260310_191306/monitor.log`
- Result on the current worktree:
  - `scan=ok count=0`
  - `wifi_mac_isr_diag label=after_scan count=39`
  - `wifi_promisc_cb_diag label=after_scan count=0`
- Then bisected the three lower legacy-runtime slices individually, keeping the same legacy ISR-send branch on:
  - timer only:
    - `logs/esp_radio_nostd_wifi_control_timer_only_20260310_190942/monitor.log`
    - `scan=ok count=0`
    - `wifi_promisc_cb_diag label=after_scan count=0`
  - scheduler only:
    - `logs/esp_radio_nostd_wifi_control_scheduler_only_20260310_191037/monitor.log`
    - `wifi_promisc_cb_diag label=after_scan count=0`
  - preempt timer only:
    - `logs/esp_radio_nostd_wifi_control_preempt_only_20260310_191133/monitor.log`
    - `scan=ok count=0`
    - `wifi_promisc_cb_diag label=after_scan count=0`
- This invalidates the earlier “legacy ISR-send alone still restores packet delivery in the current worktree” assumption as a durable conclusion.
- Practical implication:
  - the earlier positive standalone ISR-send result remains historically useful
  - but on the current worktree, that path must be treated as regressed until re-established
  - the per-slice bisect above is therefore informative, but not yet a final causal split

## 2026-03-10 Exact legacy IRQ-delivery bundle still does not restore packet delivery on the current worktree

- Re-ran the isolated current standalone with the full ISR-delivery wrapper bundle enabled:
  - `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_QUEUE_SEND_FROM_ISR_DIAG=1`
  - `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_SEMAPHORE_FROM_ISR_DIAG=1`
  - `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_TASK_YIELD_FROM_ISR_DIAG=1`
  - `MEDITAMER_WIFI_ESP_RADIO_USE_LEGACY_COEX_STATUS_GET_DIAG=1`
- Validation log:
  - `logs/esp_radio_nostd_wifi_control_irqbundle_20260310_193543/monitor_retry.log`
- Result:
  - startup still reached `begin=true`, `esp_radio_init=ok`, `wifi_new=ok`, `start=ok`
  - pre-scan raw promisc still stayed fully dark:
    - `wifi_mac_isr_diag label=after_promisc count=7`
    - `wifi_promisc_cb_diag label=after_promisc count=0`
    - `promisc_diag ... total=0 mgmt=0 ctrl=0 data=0 misc=0`
  - wrapped scan still ended at:
    - `scan=ok count=0`
    - `wifi_mac_isr_diag label=after_scan count=35`
    - `wifi_promisc_cb_diag label=after_scan count=0`
- This closes the remaining easy blob-facing ISR wrapper class on the current worktree:
  - queue-send-from-ISR semantics
  - semaphore-from-ISR semantics
  - ISR yield semantics
  - legacy `coex_status_get` fallback
- The surviving boundary is now below those wrapper hooks:
  - live `WIFI_MAC` IRQs
  - zero delivered packets to raw promisc callback
  - zero delivered packets to internal RX callbacks
  - zero scan admission
## 2026-03-10 - current ISR wake semantics still do not restore packet delivery

- Updated the current standalone `esp_radio_nostd_wifi_control` probe to count `task_yield_from_isr` alongside the existing Wi-Fi OS ISR counters.
- Revalidated the current standalone path on hardware using an `elf2image` app-only flash flow after confirming `espflash save-image` was not emitting the requested output file for this tool.
- Result from `logs/esp_radio_nostd_wifi_control_isrhooks2_20260310_200236/monitor.log`:
  - after promisc: `wifi_mac_isr=11`, `wifi_promisc_cb=0`, `queue_send_isr=0`, `task_yield_from_isr=0`
  - after scan: `wifi_mac_isr=42`, `wifi_promisc_cb=0`, `queue_send_isr=14`, `task_yield_from_isr=0`
- Patched the legacy ISR-send diagnostic branch so a successful `queue_send_from_isr()` also calls `task_yield_from_isr()` directly.
- Reused the already flashed image and captured `logs/esp_radio_nostd_wifi_control_isryield_20260310_200506/summary_postflash.txt`.
- Result from that run:
  - after promisc: `wifi_mac_isr=13`, `wifi_promisc_cb=0`, `queue_send_isr=3`, `task_yield_from_isr=6`
  - after scan: `wifi_mac_isr=45`, `wifi_promisc_cb=0`, `queue_send_isr=17`, `task_yield_from_isr=6`
  - final result still `scan=ok count=0`
- Interpretation:
- current `esp-radio` can now be shown to perform ISR queue-send plus ISR-yield activity while still delivering zero packets to the raw promiscuous callback
- this moves the remaining boundary below ISR wake semantics and above callback delivery / packet admission
- the next closest blob-facing target is sniffer/promiscuous callback storage and dispatch in the current `esp-radio` path

## 2026-03-11 - `backend_legacy_port` now uses a dedicated legacy Wi-Fi os-adapter path

- Continued the wholesale vendoring path instead of adding more mixed modern-adapter branches.
- Added a dedicated legacy Wi-Fi os-adapter backend module:
  - `vendor/esp-radio-0.17.0/src/wifi/os_adapter/legacy_backend.rs`
- Moved the `backend_legacy_port` path for these Wi-Fi callbacks out of the mixed modern adapter body and into that dedicated legacy module:
  - `task_yield_from_isr`
  - `wifi_thread_semphr_get`
  - mutex create/delete/lock/unlock
  - task create/delete/delay/current/max-priority
  - `coex_status_get`
- `vendor/esp-radio-0.17.0/src/wifi/os_adapter/mod.rs` now dispatches to `legacy_backend` for the legacy backend mode instead of directly embedding those legacy bodies inline.
- This is a structural migration step only. `cargo check` passes, but no new runtime verdict has been recorded for it yet.
- Practical implication:
  - `backend_legacy_port` is less dependent on the mixed modern Wi-Fi os-adapter body
  - the next validation should target this dedicated legacy os-adapter path through canonical firmware boot-scan, not another generic hook A/B

## 2026-03-11 - dedicated legacy Wi-Fi os-adapter path is structurally in place but does not move the init boundary

- Validated `backend_legacy_port` through canonical hostctl `flash-capture` with the dedicated legacy Wi-Fi os-adapter backend active.
- Artifact:
  - `logs/hostctl_flashcapture_backend_legacy_port_20260311_legacy_os_adapter/capture.log`
- Result:
  - runtime still reaches:
    - `legacy_port runtime_init stage=before_esp_radio_init`
    - `after_esp_radio_init`
    - `init_tasks result=ok`
    - `before_wifi_new`
    - `legacy_port_wifi_new stage=begin`
    - `validate`
    - `write_g_config`
    - `wifi_init`
    - `legacy_port_wifi_init stage=esp_wifi_init_internal.before`
  - and still stops there
- This means the dedicated legacy Wi-Fi os-adapter split is the right structural move, but it is not the missing runtime slice by itself.
- The surviving wholesale-port boundary remains:
  - inside `esp_wifi_init_internal(...)`
  - after backend/runtime bootstrap
  - after `legacy_port_wifi_new` setup
