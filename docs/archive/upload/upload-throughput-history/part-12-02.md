# Upload Throughput History (Part 12) (continued 2)

[Back to Part 12](./part-12.md).

## 2026-03-05: mode bounce + long-backoff probes did not recover all-zero discovery

Changes and runs:

- post-stop mode bounce knob (`MEDITAMER_WIFI_MODE_NULL_STA_RESET_AFTER_STOP`):
  - off:
    `logs/wifi_discovery_debug_step65_modereset_ab_off_1r_20260305_133701.log`
  - on:
    `logs/wifi_discovery_debug_step65_modereset_ab_on_1r_20260305_134131.log`
- policy backoff probe (`driver_restart_backoff_ms=8000`):
  - `logs/wifi_discovery_debug_step65_longbackoff8s_1r_20260305_134700.log`

Observed:

- mode bounce executed (`set_null_rc=0`, `set_sta_rc=0`) but both variants
  still failed with `scan_nonzero=0`.
- long backoff reduced loop count (`scan_zero=7`) but still failed with
  `scan_nonzero=0`.

Conclusion:

- neither mode bounce nor restart backoff length was sufficient to recover
  discoverability under the failing state.

## 2026-03-05: software-reset hard-guard path + host recovery classing

Firmware changes:

- terminal-only software reset hook (bounded run showed no trigger):
  - `MEDITAMER_WIFI_SOFTWARE_RESET_ON_ZERO_DISCOVERY_TERMINAL`
  - run:
    `logs/wifi_discovery_debug_step65_softreset_terminal_on_1r_20260305_135315.log`
- hard-guard software reset hook (triggered):
  - `MEDITAMER_WIFI_SOFTWARE_RESET_ON_ZERO_DISCOVERY_HARD_GUARD`
  - run:
    `logs/wifi_discovery_debug_step65_softreset_hardguard_on_1r_20260305_135854.log`
  - observed marker + reboot:
    `zero_discovery_hard_guard software_reset=true`, then
    `BOOT_RESET reason=Some(CoreSw) code=3`.

Host changes:

- discovery workflow now treats one marker-armed `UnexpectedReboot` as expected
  and re-applies `NETCFG SET` + `NET START` instead of immediate panic abort.

Validation:

- host-recovery run:
  - `logs/wifi_discovery_debug_step65_softreset_hardguard_on_hostrecover_1r_20260305_140238.log`
  - summary: `ready=true`, `scan_nonzero=1`, `ssid_seen=2`.
- short follow-up runs:
  - `logs/wifi_discovery_debug_step65_softreset_hardguard_on_hostrecover_3r_20260305_140337.log`
  - `logs/wifi_discovery_debug_step65_softreset_hardguard_on_hostrecover_3r_recover_20260305_140349.log`
  - all rounds reported ready but without scan-evidence counters
    (`scan_zero=0`, `scan_nonzero=0`, `ssid_seen=0`).

Conclusion:

- early software reset + host expected-reboot recovery is now operational.
- next validation pass should force scan-evidence telemetry while exercising
  this branch to confirm true discoverability stability (not just ready-state).
